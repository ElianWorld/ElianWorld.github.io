<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://elianworld.github.io/</id>
    <title>Rain</title>
    <updated>2021-06-17T01:55:45.146Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://elianworld.github.io/"/>
    <link rel="self" href="https://elianworld.github.io/atom.xml"/>
    <subtitle>隔壁超市薯片半价</subtitle>
    <logo>https://elianworld.github.io/images/avatar.png</logo>
    <icon>https://elianworld.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Rain</rights>
    <entry>
        <title type="html"><![CDATA[一套代码打不通APK——dependencies依赖相关]]></title>
        <id>https://elianworld.github.io/post/yYgvyddZt/</id>
        <link href="https://elianworld.github.io/post/yYgvyddZt/">
        </link>
        <updated>2021-03-16T02:13:17.000Z</updated>
        <content type="html"><![CDATA[<p>有时候APP的运行环境不同，使用的库也要不一样，一般的做法是把所有的库都添加进来，然后跟据运行环境判断一下，再调用相应的库。另外一种做法是为不同的产品编译不同的库，产品A只编译自己需要库，不需要编译对自己无用的库：<br>
假如productFlavors中有如下配置：</p>
<pre><code>productFlavors {
            appone {
            }

            apptwo {
            }
        }
</code></pre>
<p>可以在dependencies中按以下配置：</p>
<pre><code>dependencies {
    # ....
    apponeCompile '*********'
    apptwoCompile '*********'
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Cause: buildOutput.apkData must not be null问题总结]]></title>
        <id>https://elianworld.github.io/post/0z6buZbp_/</id>
        <link href="https://elianworld.github.io/post/0z6buZbp_/">
        </link>
        <updated>2021-03-03T06:38:09.000Z</updated>
        <content type="html"><![CDATA[<p>####问题：<br>
从同一个工程代码打出来第二个APP后，打包过程出现了apkData must not be null，虽然不影响打包生成APK文件，但是看见总是觉得难受，就来看下怎么解决吧：<br>
####解决方案：<br>
#####1、Clean Project<br>
正常打包输出的文件除了APK之外，还有output.json文件，里面的数据结构如下：<br>
<img src="https://elianworld.github.io//post-images/1614753919929.png" alt="" loading="lazy"><br>
path就是指向的apk的生成路径，怀疑是这里输出有问题，删除json文件之后，重新clearn project之后，打包，还是报错。</p>
<hr>
<p>#####2、删除module和主工程的的build目录后，重新rebuild打包<br>
删除json文件，重新生成的解决方案不生效之后，怀疑是本地缓存导致的部分apkData无法达到apk文件中，apk打包流程中资源的打包和代码的打包是两套不一样的流程，具体的步骤后续文章中再详细介绍。<br>
删除module和主工程build目录，rebuild，重新打包，依旧报错。。。</p>
<hr>
<p>#####3、打包最后一步修改原有指定的输出路径<br>
因为涉及到一套工程输出两个APK，就怀疑是否是另一套输出路径中的json文件影响了新APK的输出，修改输出路径，重新打包后，解决问题。<br>
####总结：<br>
查找了相关文章，大部分的buildOutput.apkData must not be null问题都可以按照步骤1或者2解决，但是因为问题出现的前置场景不一样，可能是不一样的问题导致的相同的现象，但是根本原因是一致的，看报错提示，很明显是输出的apkData为null导致的，但是即使有报错，APK也能正常生成使用，这个问题可能是gradle版本的问题。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[MAC电脑升级Big Sur后Android Studio问题踩坑录]]></title>
        <id>https://elianworld.github.io/post/8OL54oMiX/</id>
        <link href="https://elianworld.github.io/post/8OL54oMiX/">
        </link>
        <updated>2021-01-15T05:23:06.000Z</updated>
        <content type="html"><![CDATA[<h4 id="第一个问题升级之后android-studio在非主tab下打开新tab卡死的问题">第一个问题：升级之后Android Studio在非主TAB下打开新TAB卡死的问题</h4>
<p>此问题Google官方的解决方案如下：https://developer.android.com/studio/known-issues?hl=zh-cn<br>
但是应该是Android Studio版本问题没有找到对应的选项，所以就使用了命令行的解决方案，打开MAC命令行，执行以下：</p>
<pre><code>defaults write com.google.android.studio AppleWindowTabbingMode manual
</code></pre>
<p>关闭Android Studio后重启就没这个问题了</p>
<hr>
<h4 id="第二个问题升级系统之后偶然出现android-studio识别不了手机的问题">第二个问题：升级系统之后，偶然出现Android Studio识别不了手机的问题</h4>
<p>在运行程序时，插上手机，在设备列表中没有显示设备，手机上显示充电中，DEBUG模式没有问题。</p>
<p>刚开始以为adb链接出现问题，不断重启adb server，断开手机连接线，重新链接，没有效果，最后发现是电脑识别USB口的模式没有更新，断开雷电口的链接线，重新插入即可（之前版本的系统，不需要重连操作即可识别设备）【MAC BOOK的转接头是苹果官方的，与雷电口的兼容应该还行，问题必现流程：转接口接iPad后，断开iPad设备，连接手机进行ADB调试，就识别不出来了】</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android arouter路由实记]]></title>
        <id>https://elianworld.github.io/post/9sW9TzfSP/</id>
        <link href="https://elianworld.github.io/post/9sW9TzfSP/">
        </link>
        <updated>2021-01-13T07:37:54.000Z</updated>
        <content type="html"><![CDATA[<h4 id="1-不同组件的路由group必须不同">1、不同组件的路由group必须不同</h4>
<p>arouter在编译期间对工程中的不同组件的路由关系进行校验，不同组件之间、组件和主工程之间的group必须不同</p>
<h4 id="2-所有有需要路由跳转的对象需要在主工程进行注册">2、所有有需要路由跳转的对象，需要在主工程进行注册</h4>
<p>arouter不支持插件化的热加载，需要在主工程dependencies手动注册对应的组件后，才可以实现路由跳转</p>
<h4 id="3-最后在统一的资源组件中对路由关系进行维护">3、最后在统一的资源组件中对路由关系进行维护</h4>
<p>可以选择在资源组件中对工程中不同组件的路由关系进行手动维护，优势是可以添加路由的注释，便于后期维护，也可以把路由和组件间的对应关系也描述在资源组件中</p>
<h4 id="4-新添加路由后最好卸载重新运行app">4、新添加路由后，最好卸载重新运行app</h4>
<p>实际运行期间，如果不卸载重新运行，可能找不到路由报错</p>
<h4 id="5-组件资源的命名最好以group开头防止因为资源冲突导致编译错误">5、组件资源的命名最好以group开头，防止因为资源冲突导致编译错误</h4>
<p>APK在打包过程中会对资源进行统一的打包，有兴趣的可以看下APK打包流程和AAPT的实现逻辑，在实际使用过程中，为防止资源的冲突，最好将资源的命名以group为开头，保证编译不出问题</p>
<h4 id="6-如果代码编写过程中出现资源加载异常比如通过r定位不到对应的资源了最好invalidate-caches-restart">6、如果代码编写过程中出现资源加载异常，比如通过R定位不到对应的资源了，最好Invalidate Caches / Restart</h4>
<h4 id="7-使用arouter的组件中gradle文件的defaultconfig中务必添加以下设置">7、使用arouter的组件中，gradle文件的defaultConfig中务必添加以下设置</h4>
<pre><code>        javaCompileOptions {
            annotationProcessorOptions {
                arguments = [moduleName: project.getName()]
            }
        }
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android MMKV-高性能键值对存取组件]]></title>
        <id>https://elianworld.github.io/post/e9UJGvgZc/</id>
        <link href="https://elianworld.github.io/post/e9UJGvgZc/">
        </link>
        <updated>2020-12-20T11:28:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="一-基础使用">一、基础使用：</h3>
<ul>
<li>引入组件（最新版本参考<a href="https://github.com/Tencent/MMKV">官方github</a>）：</li>
</ul>
<pre><code>implementation 'com.tencent:mmkv-static:1.2.6'
</code></pre>
<ul>
<li>初始化：<br>
应用的Application中添加：</li>
</ul>
<pre><code>MMKV.initialize(this);
</code></pre>
<ul>
<li>存值：</li>
</ul>
<pre><code>MMKV kv = MMKV.defaultMMKV();
kv.encode(&quot;key&quot;, &quot;saved&quot;);
</code></pre>
<ul>
<li>取值</li>
</ul>
<pre><code>String str = kv.decodeString(&quot;key&quot;);
</code></pre>
<h3 id="二-原理">二、原理</h3>
<p>1、mmap内存映射<br>
MMKV通过内存映射mmap提供给应用程序一块儿可供读写的内存块，用户只需要关注读写数据，不需要关注数据的持久化。<br>
说到mmap，自然就想到了Android的跨进程通信组件Binder，mmap方法实际上是把设备指定的内存块直接映射到了应用程序的内存空间，Binder是将这块儿内存空间当做进程间通信的桥梁，MMKV是使用这块儿内存空间提供给用户以进行高效的读写。<br>
2、使用<a href="https://developers.google.com/protocol-buffers">谷歌protobuf协议</a>进行序列化和反序列化<br>
3、为保证写入速度，MMKV采用增量更新的机制，sharepreferences每次在commit的时候，都是对文件进行全量的更新，在数据量较大时，可能造成卡顿的现象（sharepreferences在设计之初就不是为了保存大文件），为了提升写入速度，减少全量更新的问题，MMKV会在对数据进行序列化后，append到内存末尾，读入时，使用后写入的值即可。</p>
<h3 id="三-问题">三、问题</h3>
<p>1、增量更新机制虽然在大多数情况下能保证数据写入的效率，但是在申请的内存块儿使用到了末尾时，会进行内存重整，体现在用户侧就是在发生重整时，卡顿现象更为明显。<br>
2、MMKV的机制本身保证了其自身是支持跨进程访问的，但同时又面对着多进程读写的问题，MMKV使用重新封装的文件锁来保证跨进程访问安全，具体细节：<a href="https://github.com/Tencent/MMKV/wiki/android_ipc">MMKV for Android 多进程设计与实现</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android 路由策略arouter]]></title>
        <id>https://elianworld.github.io/post/gh7GSa13m/</id>
        <link href="https://elianworld.github.io/post/gh7GSa13m/">
        </link>
        <updated>2020-12-18T07:34:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="一-基础使用">一、基础使用</h3>
<h5 id="1-dependencies添加arouter引用">1、dependencies添加arouter引用：</h5>
<pre><code>implementation 'com.alibaba:arouter-api:1.3.1'
annotationProcessor 'com.alibaba:arouter-compiler:1.1.4'
</code></pre>
<h5 id="2-defaultconfig中添加javacompileoptions">2、defaultConfig中添加javaCompileOptions：</h5>
<pre><code>javaCompileOptions {
            annotationProcessorOptions {
                arguments = [moduleName: project.getName()]
            }
        }
</code></pre>
<h5 id="3-application初始化">3、Application初始化：</h5>
<pre><code>ARouter.init(this);
</code></pre>
<h5 id="4-activity添加注解">4、Activity添加注解</h5>
<pre><code>@Route(path = &quot;/world/mainpage&quot;)
public class MainActivity extends Activity {
}
</code></pre>
<h5 id="5-使用以下方式跳转对应的page">5、使用以下方式跳转对应的page：</h5>
<pre><code>ARouter.getInstance().build(&quot;/world/setting&quot;).navigation(mContext, requestCode);
</code></pre>
<hr>
<p>注解一般由group和pageid组成<br>
添加完注解后，在app/build/generated/source/apt/appname/debug/com.alibaba.android.arouter.routes下自动生成路由文件。<br>
<img src="https://elianworld.github.io//post-images/1608278308444.png" alt="" loading="lazy"></p>
<p>ARouter$$Group$$world中存放路由表：</p>
<pre><code>public class ARouter$$Group$$world implements IRouteGroup {
  @Override
  public void loadInto(Map&lt;String, RouteMeta&gt; atlas) {
    atlas.put(&quot;/world/mainpage&quot;, RouteMeta.build(RouteType.ACTIVITY, MainActivity.class, &quot;/world/mainpage&quot;, &quot;world&quot;, null, -1, -2147483648));
    atlas.put(&quot;/world/setting&quot;, RouteMeta.build(RouteType.ACTIVITY, SettingActivity.class, &quot;/world/setting&quot;, &quot;world&quot;, null, -1, -2147483648));
  }
}
</code></pre>
<p>项目中不同的mudule间的跳转也和上述一样的方法，为方便管理，不同的module使用不同的group，可在主module中进行统一的管理。</p>
<h3 id="二-arouter初始化分析">二、arouter初始化分析</h3>
<p>Application中对ARouter进行初始化：</p>
<pre><code>ARouter.init(this);
</code></pre>
<p>ARouter中执行<code>_ARouter.init(application);</code>对_ARouter进行初始化<br>
_ARouter中执行<code>LogisticsCenter.init(mContext, executor);</code>对LogisticsCenter进行初始化，LogisticsCenter中保存了路由映射关系，在LogisticsCenter的init方法中：</p>
<pre><code>    public synchronized static void init(Context context, ThreadPoolExecutor tpe) throws HandlerException {
        mContext = context;
        executor = tpe;

        try {
            long startInit = System.currentTimeMillis();
            //billy.qi modified at 2017-12-06
            //load by plugin first
            loadRouterMap();
            if (registerByPlugin) {
                logger.info(TAG, &quot;Load router map by arouter-auto-register plugin.&quot;);
            } else {
                Set&lt;String&gt; routerMap;

                // It will rebuild router map every times when debuggable.
                if (ARouter.debuggable() || PackageUtils.isNewVersion(context)) {
                    logger.info(TAG, &quot;Run with debug mode or new install, rebuild router map.&quot;);
                    // These class was generate by arouter-compiler.
                    routerMap = ClassUtils.getFileNameByPackageName(mContext, ROUTE_ROOT_PAKCAGE);
                    if (!routerMap.isEmpty()) {
                        context.getSharedPreferences(AROUTER_SP_CACHE_KEY, Context.MODE_PRIVATE).edit().putStringSet(AROUTER_SP_KEY_MAP, routerMap).apply();
                    }

                    PackageUtils.updateVersion(context);    // Save new version name when router map update finish.
                } else {
                    logger.info(TAG, &quot;Load router map from cache.&quot;);
                    routerMap = new HashSet&lt;&gt;(context.getSharedPreferences(AROUTER_SP_CACHE_KEY, Context.MODE_PRIVATE).getStringSet(AROUTER_SP_KEY_MAP, new HashSet&lt;String&gt;()));
                }

                logger.info(TAG, &quot;Find router map finished, map size = &quot; + routerMap.size() + &quot;, cost &quot; + (System.currentTimeMillis() - startInit) + &quot; ms.&quot;);
                startInit = System.currentTimeMillis();

                for (String className : routerMap) {
                    if (className.startsWith(ROUTE_ROOT_PAKCAGE + DOT + SDK_NAME + SEPARATOR + SUFFIX_ROOT)) {
                        // This one of root elements, load root.
                        ((IRouteRoot) (Class.forName(className).getConstructor().newInstance())).loadInto(Warehouse.groupsIndex);
                    } else if (className.startsWith(ROUTE_ROOT_PAKCAGE + DOT + SDK_NAME + SEPARATOR + SUFFIX_INTERCEPTORS)) {
                        // Load interceptorMeta
                        ((IInterceptorGroup) (Class.forName(className).getConstructor().newInstance())).loadInto(Warehouse.interceptorsIndex);
                    } else if (className.startsWith(ROUTE_ROOT_PAKCAGE + DOT + SDK_NAME + SEPARATOR + SUFFIX_PROVIDERS)) {
                        // Load providerIndex
                        ((IProviderGroup) (Class.forName(className).getConstructor().newInstance())).loadInto(Warehouse.providersIndex);
                    }
                }
            }

            logger.info(TAG, &quot;Load root element finished, cost &quot; + (System.currentTimeMillis() - startInit) + &quot; ms.&quot;);

            if (Warehouse.groupsIndex.size() == 0) {
                logger.error(TAG, &quot;No mapping files were found, check your configuration please!&quot;);
            }

            if (ARouter.debuggable()) {
                logger.debug(TAG, String.format(Locale.getDefault(), &quot;LogisticsCenter has already been loaded, GroupIndex[%d], InterceptorIndex[%d], ProviderIndex[%d]&quot;, Warehouse.groupsIndex.size(), Warehouse.interceptorsIndex.size(), Warehouse.providersIndex.size()));
            }
        } catch (Exception e) {
            throw new HandlerException(TAG + &quot;ARouter init logistics center exception! [&quot; + e.getMessage() + &quot;]&quot;);
        }
    }
</code></pre>
<p>初始化路由表，将项目中添加的注解初始化到Warehouse.groupsIndex中，打断点可以看到：<br>
<img src="https://elianworld.github.io//post-images/1608279198815.png" alt="" loading="lazy"></p>
<p>在ARouter中_ARouter初始化后，对拦截器进行初始化：</p>
<pre><code>_ARouter.afterInit();
</code></pre>
<p>afterInit中初始化interceptorService拦截器</p>
<pre><code>    static void afterInit() {
        // Trigger interceptor init, use byName.
        interceptorService = (InterceptorService) ARouter.getInstance().build(&quot;/arouter/service/interceptor&quot;).navigation();
    }
</code></pre>
<p>interceptorService用于在arouter的navigation中对用户的调用进行拦截处理</p>
<h3 id="三-arouter跳转">三、arouter跳转</h3>
<p>一个一般的跳转方法：</p>
<pre><code>ARouter.getInstance().build(&quot;/world/setting&quot;).navigation(this, 2);
</code></pre>
<p>主要包含两个方法build和navigation</p>
<h5 id="build方法">build方法：</h5>
<p>入参为目标页面的pageid<br>
方法：</p>
<pre><code>    public Postcard build(String path) {
        return _ARouter.getInstance().build(path);
    }
</code></pre>
<p>_ARouter的build方法：</p>
<pre><code>    protected Postcard build(String path) {
        if (TextUtils.isEmpty(path)) {
            throw new HandlerException(Consts.TAG + &quot;Parameter is invalid!&quot;);
        } else {
            PathReplaceService pService = ARouter.getInstance().navigation(PathReplaceService.class);
            if (null != pService) {
                path = pService.forString(path);
            }
            return build(path, extractGroup(path));
        }
    }
</code></pre>
<p>在<code>build(path, extractGroup(path));</code>中，返回一个Postcard对象，其中保存了目标的pageid和group信息。<br>
Postcard是一次跳转过程中的一次路由请求，包含本次路由的基本信息</p>
<h5 id="navigation方法">navigation方法：</h5>
<p>navigation方法的参数包含Context和requestCode（可选）<br>
最终调用了_ARouter的_navigation方法：</p>
<pre><code>private Object _navigation(final Context context, final Postcard postcard, final int requestCode, final NavigationCallback callback)
</code></pre>
<p>context为入参中的Context，postcard为通过build获取到的路由对象<br>
在该方法中我们看到根据postcard的type做了不同处理，包含以下类型：</p>
<pre><code>    ACTIVITY(0, &quot;android.app.Activity&quot;),
    SERVICE(1, &quot;android.app.Service&quot;),
    PROVIDER(2, &quot;com.alibaba.android.arouter.facade.template.IProvider&quot;),
    CONTENT_PROVIDER(-1, &quot;android.app.ContentProvider&quot;),
    BOARDCAST(-1, &quot;&quot;),
    METHOD(-1, &quot;&quot;),
    FRAGMENT(-1, &quot;android.app.Fragment&quot;),
</code></pre>
<p>我们这里只做Activity间的跳转</p>
<pre><code>                // Build intent
                final Intent intent = new Intent(currentContext, postcard.getDestination());
                intent.putExtras(postcard.getExtras());

                // Set flags.
                int flags = postcard.getFlags();
                if (-1 != flags) {
                    intent.setFlags(flags);
                } else if (!(currentContext instanceof Activity)) {    // Non activity, need less one flag.
                    intent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                }

                // Navigation in main looper.
                new Handler(Looper.getMainLooper()).post(new Runnable() {
                    @Override
                    public void run() {
                        if (requestCode &gt; 0) {  // Need start for result
                            ActivityCompat.startActivityForResult((Activity) currentContext, intent, requestCode, postcard.getOptionsBundle());
                        } else {
                            ActivityCompat.startActivity(currentContext, intent, postcard.getOptionsBundle());
                        }

                        if ((0 != postcard.getEnterAnim() || 0 != postcard.getExitAnim()) &amp;&amp; currentContext instanceof Activity) {    // Old version.
                            ((Activity) currentContext).overridePendingTransition(postcard.getEnterAnim(), postcard.getExitAnim());
                        }

                        if (null != callback) { // Navigation over.
                            callback.onArrival(postcard);
                        }
                    }
                });
</code></pre>
<p>从postcard中获取传入的参数，上述示例中没有添加页面跳转的参数，从这部分代码我们可以看出来，在arouter中，使用mBundle保存了传入的参数，在构建跳转Intent时，通过<code>intent.putExtras(postcard.getExtras());</code>重置参数到intent中，通过<code>intent.setFlags(flags);</code>将预设的跳转标识传入intent中<br>
然后通过Looper.getMainLooper()，在UI线程中进行跳转处理，从代码中我们可以看到，arouter同时实现了overridePendingTransition的封装，支持添加跳转的动画。<br>
currentContext在传入的context为空时获取了application的context。</p>
<p>arouter在_ARouter的_navigation方法之前，通过<code>LogisticsCenter.completion(postcard);</code>将path与目标class建立对应关系：<br>
completion方法中通过<code>RouteMeta routeMeta = Warehouse.routes.get(postcard.getPath());</code>获取path对应的路由关系，routeMeta中就包含了目标Activity的路径。<br>
completion后续将routeMeta中的信息转储到postcard中，最主要的：</p>
<pre><code>postcard.setDestination(routeMeta.getDestination());
</code></pre>
<p>然后在上文构建intent的过程中将destination传入：</p>
<pre><code>final Intent intent = new Intent(currentContext, postcard.getDestination());
</code></pre>
<h3 id="四-arouter特性分析">四、arouter特性分析</h3>
<ul>
<li>在多module中使用，支持组件化架构方案</li>
<li>由arouter的postcard中type类型分析，可以知道，arouter支持多种类型，包含常见的activity、Service、ContentProvider、Fragment等等</li>
<li>由arouter维护路由表，减少了工程中路由关系的维护</li>
<li>使用注解实现了分布式路由管理，编译期间处理注解，生成路由文件，不影响运行时的性能</li>
<li>实现了自定义的拦截器，可以对应用中的路由进行拦截分析</li>
</ul>
<h4 id="ps使用arouter过程中不要忘记在manifest文件中注册activity和service等组件arouter不会处理组件的注册">PS：使用arouter过程中，不要忘记在Manifest文件中注册Activity和Service等组件，ARouter不会处理组件的注册。</h4>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android 应用程序启动流程]]></title>
        <id>https://elianworld.github.io/post/CwcvnCYMn/</id>
        <link href="https://elianworld.github.io/post/CwcvnCYMn/">
        </link>
        <updated>2020-12-18T02:41:04.000Z</updated>
        <content type="html"><![CDATA[<h4 id="android系统是基于linux的运行在其上的应用程序不只是apk一种类型下面所述应用程序的启动流程特指apk类型应用程序的启动流程">Android系统是基于Linux的，运行在其上的应用程序不只是APK一种类型，下面所述应用程序的启动流程特指APK类型应用程序的启动流程。</h4>
<hr>
<p>APK类型应用程序在系统中有以下两种启动方式：</p>
<ul>
<li>在Launcher中点击应用程序图标启动。</li>
<li>通过startActivity启动</li>
</ul>
<p>两种启动方式流程基本一致，最终都会调用ActivityManagerService的startActivity来完成。</p>
<p>AMS内部调用一系列以startActivity开头的方法处理Activity启动之前的判断和准备工作<br>
具体流程如下：<br>
<img src="https://elianworld.github.io//post-images/1608260376437.png" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android 启动速度优化]]></title>
        <id>https://elianworld.github.io/post/GI-bi9PCc/</id>
        <link href="https://elianworld.github.io/post/GI-bi9PCc/">
        </link>
        <updated>2020-12-11T03:46:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="文章转载自深入探索android启动速度优化">文章转载自<a href="https://jsonchao.github.io/2019/11/10/%E6%B7%B1%E5%85%A5%E6%8E%A2%E7%B4%A2Android%E5%90%AF%E5%8A%A8%E9%80%9F%E5%BA%A6%E4%BC%98%E5%8C%96/">深入探索Android启动速度优化</a></h3>
<hr>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><h3 id="成为一名优秀的Android开发，需要一份完备的知识体系，在这里，让我们一起成长为自己所想的那样-。"><a href="#成为一名优秀的Android开发，需要一份完备的知识体系，在这里，让我们一起成长为自己所想的那样-。" class="headerlink" title="成为一名优秀的Android开发，需要一份完备的知识体系，在这里，让我们一起成长为自己所想的那样~。"></a>成为一名优秀的Android开发，需要一份完备的<a href="https://github.com/JsonChao/Awesome-Android-Exercise" target="_blank" rel="external">知识体系</a>，在这里，让我们一起成长为自己所想的那样~。</h3><h2 id="一、启动优化的意义"><a href="#一、启动优化的意义" class="headerlink" title="一、启动优化的意义"></a>一、启动优化的意义</h2><p>如果我们去一家餐厅吃饭，在点餐的时候等了半天都没有服务人员过来，可能就没有耐心等待直接走了。</p>
<p>对于App来说，也是同样如此，如果用户点击App后，App半天都打不开，用户就可能失去耐心卸载应用。</p>
<p>启动速度是用户对我们App的第一体验，打开应用后才能去使用其中提供的强大功能，就算我们应用的内部界面设计的再精美，功能再强大，如果启动速度过慢，用户第一印象就会很差。</p>
<p>因此，拯救App的启动速度，迫在眉睫。下面，我们来逐步深入探索提升Android App启动速度的奥秘。</p>
<h2 id="二、应用启动流程"><a href="#二、应用启动流程" class="headerlink" title="二、应用启动流程"></a>二、应用启动流程</h2><h3 id="1-、应用启动的类型"><a href="#1-、应用启动的类型" class="headerlink" title="1 、应用启动的类型"></a>1 、应用启动的类型</h3><h4 id="冷启动"><a href="#冷启动" class="headerlink" title="冷启动"></a>冷启动</h4><p>从点击应用图标到UI界面完全显示且用户可操作的全部过程。</p>
<h5 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h5><p>耗时最多，衡量标准</p>
<h5 id="启动流程"><a href="#启动流程" class="headerlink" title="启动流程"></a>启动流程</h5><p>Click Event    -&gt;     IPC     -&gt;     Process.start     -&gt;      ActivityThread -&gt;    bindApplication      -&gt;      LifeCycle    -&gt;     ViewRootImpl</p>
<h4 id="热启动"><a href="#热启动" class="headerlink" title="热启动"></a>热启动</h4><p>因为会从已有的应用进程启动，所以不会再创建和初始化Application，只会重新创建并初始化Activity。</p>
<h5 id="特点-1"><a href="#特点-1" class="headerlink" title="特点"></a>特点</h5><p>耗时较少</p>
<h5 id="启动流程-1"><a href="#启动流程-1" class="headerlink" title="启动流程"></a>启动流程</h5><p>LifeCycle    -&gt;     ViewRootImpl</p>
<h5 id="ViewRootImpl"><a href="#ViewRootImpl" class="headerlink" title="ViewRootImpl"></a>ViewRootImpl</h5><p>ViewRoot是GUI管理系统与GUI呈现系统之间的桥梁。每一个ViewRootImpl关联一个Window，<br>ViewRootImpl最终会通过它的setView方法绑定Window所对应的View，并通过其performTraversals方法对View进行布局、测量和绘制。</p>
<h2 id="三、启动耗时检测"><a href="#三、启动耗时检测" class="headerlink" title="三、启动耗时检测"></a>三、启动耗时检测</h2><h3 id="1、查看Logcat"><a href="#1、查看Logcat" class="headerlink" title="1、查看Logcat"></a>1、查看Logcat</h3><p>在Android Studio Logcat中过滤关键字“Displayed”，可以看到对应的冷启动耗时日志。</p>
<h3 id="2、adb-shell"><a href="#2、adb-shell" class="headerlink" title="2、adb shell"></a>2、adb shell</h3><p>使用adb shell获取应用的启动时间</p>
<pre><code>// 其中的AppstartActivity全路径可以省略前面的packageName
adb shell am start -W [packageName]/[AppstartActivity全路径]
</code></pre><p>执行后会得到三个时间：ThisTime、TotalTime和WaitTime，详情如下：</p>
<h4 id="ThisTime"><a href="#ThisTime" class="headerlink" title="ThisTime"></a>ThisTime</h4><p>最后一个Activity启动耗时。</p>
<h4 id="TotalTime"><a href="#TotalTime" class="headerlink" title="TotalTime"></a>TotalTime</h4><p>所有Activity启动耗时。</p>
<h4 id="WaitTime"><a href="#WaitTime" class="headerlink" title="WaitTime"></a>WaitTime</h4><p>AMS启动Activity的总耗时。</p>
<p>一般查看得到的TotalTime，即应用的启动时间，包括创建进程 + Application初始化 + Activity初始化到界面显示的过程。</p>
<h4 id="特点："><a href="#特点：" class="headerlink" title="特点："></a>特点：</h4><ul>
<li>1、线下使用方便，不能带到线上。</li>
<li>2、非严谨、精确时间。</li>
</ul>
<h3 id="3、代码打点（函数插桩）"><a href="#3、代码打点（函数插桩）" class="headerlink" title="3、代码打点（函数插桩）"></a>3、代码打点（函数插桩）</h3><p>可以写一个统计耗时的工具类来记录整个过程的耗时情况。其中需要注意的有：</p>
<ul>
<li>在上传数据到服务器时建议根据用户ID的尾号来抽样上报。</li>
<li>在项目中核心基类的关键回调函数和核心方法中加入打点。</li>
</ul>
<p>代码如下：</p>
<pre><code>/**
* 耗时监视器对象，记录整个过程的耗时情况，可以用在很多需要统计的地方，比如Activity的启动耗时和Fragment的启动耗时。
*/
public class TimeMonitor {
<pre><code>private final String TAG = TimeMonitor.class.getSimpleName();
private int mMonitord = -1;

// 保存一个耗时统计模块的各种耗时，tag对应某一个阶段的时间
private HashMap&amp;lt;String, Long&amp;gt; mTimeTag = new HashMap&amp;lt;&amp;gt;();
private long mStartTime = 0;

public TimeMonitor(int mMonitorId) {
    Log.d(TAG, &quot;init TimeMonitor id: &quot; + mMonitorId);
    this.mMonitorId = mMonitorId;
}

public int getMonitorId() {
    return mMonitorId;
}

public void startMonitor() {
    // 每次重新启动都把前面的数据清除，避免统计错误的数据
    if (mTimeTag.size() &amp;gt; 0) {
    mTimeTag.clear();
    }
    mStartTime = System.currentTimeMillis();
}

/**
* 每打一次点，记录某个tag的耗时
*/
public void recordingTimeTag(String tag) {
    // 若保存过相同的tag，先清除
    if (mTimeTag.get(tag) != null) {
        mTimeTag.remove(tag);
    }
    long time = System.currentTimeMillis() - mStartTime;
    Log.d(TAG, tag + &quot;: &quot; + time);
    mTimeTag.put(tag, time);
}

public void end(String tag, boolean writeLog) {
    recordingTimeTag(tag);
    end(writeLog);
}

public void end(boolean writeLog) {
    if (writeLog) {
        //写入到本地文件
    }
}

public HashMap&amp;lt;String, Long&amp;gt; getTimeTags() {
    return mTimeTag;
}
</code></pre>
<p>}<br>
</code></pre><p>为了使代码更好管理，定义一个打点配置类：</p></p>
<pre><code>/**
* 打点配置类，用于统计各阶段的耗时，便于代码的维护和管理。
*/
public final class TimeMonitorConfig {

    // 应用启动耗时
    public static final int TIME_MONITOR_ID_APPLICATION_START = 1;
}
</code></pre><p>因为，耗时统计可能会在多个模块和类中需要打点，所以需要一个单例类来管理各个耗时统计的数据：</p>
<pre><code>/**
* 采用单例管理各个耗时统计的数据。
*/
public class TimeMonitorManager {

    private static TimeMonitorManager mTimeMonitorManager = null;
private HashMap&lt;Integer, TimeMonitor&gt; mTimeMonitorMap = null;

    public synchronized static TimeMonitorManager getInstance() {
        if (mTimeMonitorManager == null) {
            mTimeMonitorManager = new TimeMonitorManager();
        }
        return mTimeMonitorManager;
    }

    public TimeMonitorManager() {
        this.mTimeMonitorMap = new HashMap&lt;Integer, TimeMonitor&gt;();
    }

    /**
     * 初始化打点模块
    */
    public void resetTimeMonitor(int id) {
        if (mTimeMonitorMap.get(id) != null) {
            mTimeMonitorMap.remove(id);
        }
        getTimeMonitor(id);
    }

    /**
    * 获取打点器
    */
    public TimeMonitor getTimeMonitor(int id) {
        TimeMonitor monitor = mTimeMonitorMap.get(id);
        if (monitor == null) {
            monitor = new TimeMonitor(id);
            mTimeMonitorMap.put(id, monitor);
        }
        return monitor;
    }
}
</code></pre><p>主要在以下几个方面需要打点：</p>
<ul>
<li>应用程序的生命周期节点。</li>
<li>启动时需要初始化的重要方法，如数据库初始化，读取本地的一些数据。</li>
<li>其他耗时的一些算法。</li>
</ul>
<p>例如，启动时在Application和第一个Activity加入打点统计：</p>
<p><strong>Application：</strong></p>
<pre><code>@Override
protected void attachBaseContext(Context base) {
    super.attachBaseContext(base);
    TimeMonitorManager.getInstance().resetTimeMonitor(TimeMonitorConfig.TIME_MONITOR_ID_APPLICATION_START);
}
<p>@Override<br>
public void onCreate() {<br>
super.onCreate();<br>
SoLoader.init(this, /* native exopackage */ false);<br>
TimeMonitorManager.getInstance().getTimeMonitor(TimeMonitorConfig.TIME_MONITOR_ID_APPLICATION_START).recordingTimeTag(&quot;Application-onCreate&quot;);<br>
}<br>
</code></pre><p><strong>第一个Activity：</strong></p></p>
<pre><code>@Override
protected void onCreate(Bundle savedInstanceState) {
    TimeMonitorManager.getInstance().getTimeMonitor(TimeMonitorConfig.TIME_MONITOR_ID_APPLICATION_START).recordingTimeTag("SplashActivity-onCreate");
    super.onCreate(savedInstanceState);

    initData();

    TimeMonitorManager.getInstance().getTimeMonitor(TimeMonitorConfig.TIME_MONITOR_ID_APPLICATION_START).recordingTimeTag("SplashActivity-onCreate-Over");
}

@Override
protected void onStart() {
    super.onStart();
    TimeMonitorManager.getInstance().getTimeMonitor(TimeMonitorConfig.TIME_MONITOR_ID_APPLICATION_START).end("SplashActivity-onStart", false);
}
</code></pre><h4 id="特点-2"><a href="#特点-2" class="headerlink" title="特点"></a>特点</h4><p>精确，可带到线上，推荐使用。</p>
<h4 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h4><ul>
<li><p>在上传数据到服务器时建议根据用户ID的尾号来抽样上报。</p>
</li>
<li><p>onWindowFocusChanged只是首帧时间，App启动完成的结束点应该是真实数据展示出来的时候，如列表第一条数据展示，记得使用getViewTreeObserver().addOnPreDrawListener()，它会把任务延迟到列表显示后再执行。</p>
</li>
</ul>
<h4 id="AOP-Aspect-Oriented-Programming-打点"><a href="#AOP-Aspect-Oriented-Programming-打点" class="headerlink" title="AOP(Aspect Oriented Programming)打点"></a>AOP(Aspect Oriented Programming)打点</h4><p>面向切面编程，通过预编译和运行期动态代理实现程序功能统一维护的一种技术。</p>
<h5 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h5><p>利用AOP可以对业务逻辑的各个部分进行隔离，从而使得业务逻辑各部分之间的耦合性降低，提高程序的可重用性，同时大大提高了开发效率。</p>
<h5 id="AOP核心概念"><a href="#AOP核心概念" class="headerlink" title="AOP核心概念"></a>AOP核心概念</h5><p>1、横切关注点</p>
<p>对哪些方法进行拦截，拦截后怎么处理。</p>
<p>2、切面（Aspect）</p>
<p>类是对物体特征的抽象，切面就是对横切关注点的抽象。</p>
<p>3、连接点（JoinPoint）</p>
<p>被拦截到的点（方法、字段、构造器）。</p>
<p>4、切入点（PointCut）</p>
<p>对JoinPoint进行拦截的定义。</p>
<p>5、通知（Advice）</p>
<p>拦截到JoinPoint后要执行的代码，分为前置、后置、环绕三种类型。</p>
<h5 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h5><p>首先，为了在Android使用AOP埋点需要引入AspectJ，在项目根目录的build.gradle下加入：</p>
<pre><code>classpath 'com.hujiang.aspectjx:gradle-android-plugin- aspectjx:2.0.0'
</code></pre><p>然后，在app目录下的build.gradle下加入：</p>
<pre><code>apply plugin: 'android-aspectjx'
implement 'org.aspectj:aspectjrt:1.8.+'
</code></pre><h5 id="AOP埋点实战"><a href="#AOP埋点实战" class="headerlink" title="AOP埋点实战"></a>AOP埋点实战</h5><p>JoinPoint一般定位在如下位置</p>
<ul>
<li>1、函数调用</li>
<li>2、获取、设置变量</li>
<li>3、类初始化</li>
</ul>
<p>使用PointCut对我们指定的连接点进行拦截，通过Advice，就可以拦截到JoinPoint后要执行的代码。Advice通常有以下几种类型：</p>
<ul>
<li>1、Before：PointCut之前执行</li>
<li>2、After：PointCut之后执行</li>
<li>3、Around：PointCut之前、之后分别执行</li>
</ul>
<p>首先，我们举一个小栗子：</p>
<pre><code>@Before("execution(* android.app.Activity.on**(..))")
public void onActivityCalled(JoinPoint joinPoint) throws Throwable {
...
}
</code></pre><p>在execution中的是一个匹配规则，第一个*代表匹配任意的方法返回值，后面的语法代码匹配所有Activity中on开头的方法。</p>
<p>处理Join Point的类型：</p>
<ul>
<li>1、call：插入在函数体里面</li>
<li>2、execution：插入在函数体外面</li>
</ul>
<p><strong>如何统计Application中的所有方法耗时？</strong></p>
<pre><code>@Aspect
public class ApplicationAop {
<pre><code>@Around(&quot;call (* com.json.chao.application.BaseApplication.**(..))&quot;)
public void getTime(ProceedingJoinPoint joinPoint) {
Signature signature = joinPoint.getSignature();
String name = signature.toShortString();
long time = System.currentTimeMillis();
try {
    joinPoint.proceed();
} catch (Throwable throwable) {
    throwable.printStackTrace();
}
Log.i(TAG, name + &quot; cost&quot; +     (System.currentTimeMillis() - time));
}
</code></pre>
<p>}<br>
</code></pre><p><strong>注意</strong></p></p>
<p>当Action为Before、After时，方法入参为JoinPoint。<br>当Action为Around时，方法入参为ProceedingPoint。</p>
<p><strong>Around和Before、After的最大区别:</strong></p>
<p>ProceedingPoint不同于JoinPoint，其提供了proceed方法执行目标方法。</p>
<p><strong>总结AOP特性：</strong></p>
<ul>
<li>1、无侵入性</li>
<li>2、修改方便</li>
</ul>
<h3 id="4、启动速度分析工具-—-TraceView"><a href="#4、启动速度分析工具-—-TraceView" class="headerlink" title="4、启动速度分析工具 — TraceView"></a>4、启动速度分析工具 — TraceView</h3><h4 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h4><ul>
<li>1、代码中添加：Debug.startMethodTracing()、检测方法、Debug.stopMethodTracing()。（需要使用adb pull将生成的**.trace文件导出到电脑，然后使用Android Studio的Profiler加载）</li>
<li>2、打开Profiler  -&gt;  CPU   -&gt;    点击 Record   -&gt;  点击 Stop  -&gt;  查看Profiler下方Top Down/Bottom Up 区域找出耗时的热点方法。</li>
</ul>
<h4 id="Profile-CPU"><a href="#Profile-CPU" class="headerlink" title="Profile CPU"></a>Profile CPU</h4><h5 id="1、Trace-types"><a href="#1、Trace-types" class="headerlink" title="1、Trace types"></a>1、Trace types</h5><p><strong>Trace Java Methods</strong></p>
<p>会记录每个方法的时间、CPU信息。对运行时性能影响较大。</p>
<p><strong>Sample Java Methods</strong></p>
<p>相比于Trace Java Methods会记录每个方法的时间、CPU信息，它会在应用的Java代码执行期间频繁捕获应用的调用堆栈，对运行时性能的影响比较小，能够记录更大的数据区域。</p>
<p><strong>Sample C/C++ Functions</strong></p>
<p>需部署到Android 8.0及以上设备，内部使用simpleperf跟踪应用的native代码，也可以命令行使用simpleperf。</p>
<p><strong>Trace System Calls</strong></p>
<ul>
<li>检查应用与系统资源的交互情况。</li>
<li>查看所有核心的CPU瓶。</li>
<li>内部采用systrace，也可以使用systrace命令。</li>
</ul>
<h5 id="2、Event-timeline"><a href="#2、Event-timeline" class="headerlink" title="2、Event timeline"></a>2、Event timeline</h5><p>显示应用程序中在其生命周期中转换不同状态的活动，如用户交互、屏幕旋转事件等。</p>
<h5 id="3、CPU-timeline"><a href="#3、CPU-timeline" class="headerlink" title="3、CPU timeline"></a>3、CPU timeline</h5><p>显示应用程序实时CPU使用率、其它进程实时CPU使用率、应用程序使用的线程总数。</p>
<h5 id="4、Thread-activity-timeline"><a href="#4、Thread-activity-timeline" class="headerlink" title="4、Thread activity timeline"></a>4、Thread activity timeline</h5><p>列出应用程序进程中的每个线程，并使用了不同的颜色在其时间轴上指示其活动。</p>
<ul>
<li>绿色：线程处于活动状态或准备好使用CPU。</li>
<li>黄色：线程正等待IO操作。（重要）</li>
<li>灰色：线程正在睡眠，不消耗CPU时间。</li>
</ul>
<h5 id="Profile提供的检查跟踪数据窗口有四种"><a href="#Profile提供的检查跟踪数据窗口有四种" class="headerlink" title="Profile提供的检查跟踪数据窗口有四种"></a>Profile提供的检查跟踪数据窗口有四种</h5><p><strong>1、Call Chart</strong></p>
<p>提供函数跟踪数据的图形表示形式。</p>
<ul>
<li>水平轴：表示调用的时间段和时间。</li>
<li>垂直轴：显示被调用方。</li>
<li>橙色：系统API。</li>
<li>绿色：应用自有方法</li>
<li>蓝色：第三方API（包括Java API）</li>
</ul>
<p>提示：右键点击Jump to source跳转至指定函数。</p>
<p><strong>2、Flame Chart</strong></p>
<p>将具有相同调用方顺序的完全相同的方法收集起来。</p>
<ul>
<li>水平轴：执行每个方法的相对时间量。</li>
<li>垂直轴：显示被调用方。</li>
</ul>
<p><strong>注意：看顶层的哪个函数占据的宽度最大（平顶），可能存在性能问题。</strong></p>
<p><strong>3、Top Down</strong></p>
<ul>
<li>递归调用列表，提供self、children、total时间和比率来表示被调用的函数信息。</li>
<li>Flame Chart是Top Down列表数据的图形化。</li>
</ul>
<p><strong>4、Bottom Up</strong> </p>
<ul>
<li>展开函数会显示其调用方。</li>
<li>按照消耗CPU时间由多到少的顺序对函数排序。</li>
</ul>
<p><strong>注意点：</strong></p>
<ul>
<li>Wall Clock Time：程序执行时间。</li>
<li>Thread Time：CPU执行的时间。</li>
</ul>
<h4 id="TraceView小结"><a href="#TraceView小结" class="headerlink" title="TraceView小结"></a>TraceView小结</h4><h5 id="特点-3"><a href="#特点-3" class="headerlink" title="特点"></a>特点</h5><ul>
<li>1、图形的形式展示执行时间、调用栈等。</li>
<li>2、信息全面，包含所有线程。</li>
<li>3、运行时开销严重，整体都会变慢，得出的结果并不真实。</li>
<li>4、找到最耗费时间的路径：Flame Chart、Top Down。</li>
<li>5、找到最耗费时间的节点：Bottom Up。</li>
</ul>
<h5 id="作用-1"><a href="#作用-1" class="headerlink" title="作用"></a>作用</h5><p>主要做热点分析，得到两种数据：</p>
<ul>
<li>单次执行最耗时的方法。</li>
<li>执行次数最多的方法。</li>
</ul>
<h3 id="5、启动速度分析工具-—-Systrace"><a href="#5、启动速度分析工具-—-Systrace" class="headerlink" title="5、启动速度分析工具 — Systrace"></a>5、启动速度分析工具 — Systrace</h3><h4 id="使用方式：代码插桩"><a href="#使用方式：代码插桩" class="headerlink" title="使用方式：代码插桩"></a>使用方式：代码插桩</h4><p>定义Trace静态工厂类，将Trace.begainSection()，Trace.endSection()封装成i、o方法，然后再在想要分析的方法前后进行插桩即可。</p>
<p>在命令行下执行systrace.py脚本：</p>
<pre><code>python /Users/quchao/Library/Android/sdk/platform-tools/systrace/systrace.py -t 20 sched gfx view wm am app webview -a "com.wanandroid.json.chao" -o ~/Documents/open-project/systrace_data/wanandroid_start_1.html
</code></pre><p>具体参数含义如下：</p>
<ul>
<li>-t：指定统计时间为20s。</li>
<li>shced：cpu调度信息。</li>
<li>gfx：图形信息。</li>
<li>view：视图。</li>
<li>wm：窗口管理。</li>
<li>am：活动管理。</li>
<li>app：应用信息。</li>
<li>webview：webview信息。</li>
<li>-a：指定目标应用程序的包名。</li>
<li>-o：生成的systrace.html文件。</li>
</ul>
<h5 id="如何查看数据？"><a href="#如何查看数据？" class="headerlink" title="如何查看数据？"></a>如何查看数据？</h5><p>在UIThread一栏可以看到核心的系统方法时间区域和我们自己使用代码插桩捕获的方法时间区域。</p>
<h4 id="Systrace原理"><a href="#Systrace原理" class="headerlink" title="Systrace原理"></a>Systrace原理</h4><ul>
<li>在系统的一些关键链路（如SystemServcie、虚拟机、Binder驱动）插入一些信息（Label）;</li>
<li>通过Label的开始和结束来确定某个核心过程的执行时间;<br>把这些Label信息收集起来得到系统关键路径的运行时间信息，最后得到整个系统的运行性能信息;</li>
<li>Android Framework里面一些重要的模块都插入了label信息，用户App中可以添加自定义的Lable。</li>
</ul>
<h4 id="Systrace小结"><a href="#Systrace小结" class="headerlink" title="Systrace小结"></a>Systrace小结</h4><h5 id="特性"><a href="#特性" class="headerlink" title="特性"></a>特性</h5><ul>
<li>结合Android内核的数据，生成Html报告。</li>
<li>系统版本越高，Android Framework中添加的系统可用Label就越多，能够支持和分析的系统模块也就越多。</li>
<li>必须手动缩小范围，会帮助你加速收敛问题的分析过程，进而快速地定位和解决问题。</li>
</ul>
<h5 id="作用-2"><a href="#作用-2" class="headerlink" title="作用"></a>作用</h5><ul>
<li>主要用于分析绘制性能方面的问题。</li>
<li>分析系统关键方法和应用方法耗时。</li>
</ul>
<h3 id="6、启动监控"><a href="#6、启动监控" class="headerlink" title="6、启动监控"></a>6、启动监控</h3><h4 id="1、实验室监控：视频录制"><a href="#1、实验室监控：视频录制" class="headerlink" title="1、实验室监控：视频录制"></a>1、实验室监控：视频录制</h4><ul>
<li>80%绘制</li>
<li>图像识别</li>
</ul>
<h5 id="注意-1"><a href="#注意-1" class="headerlink" title="注意"></a>注意</h5><p>覆盖高中低端机型不同的场景。</p>
<h4 id="2、线上监控"><a href="#2、线上监控" class="headerlink" title="2、线上监控"></a>2、线上监控</h4><h5 id="目标"><a href="#目标" class="headerlink" title="目标"></a>目标</h5><p>需要准确地统计启动耗时。</p>
<h5 id="1、启动结束的统计时机"><a href="#1、启动结束的统计时机" class="headerlink" title="1、启动结束的统计时机"></a>1、启动结束的统计时机</h5><p>是否是使用界面显示且用户真正可以操作的时间作为启动结束时间。</p>
<h5 id="2、启动时间扣除逻辑"><a href="#2、启动时间扣除逻辑" class="headerlink" title="2、启动时间扣除逻辑"></a>2、启动时间扣除逻辑</h5><p>闪屏、广告和新手引导这些时间都应该从启动时间里扣除。</p>
<h5 id="3、启动排除逻辑"><a href="#3、启动排除逻辑" class="headerlink" title="3、启动排除逻辑"></a>3、启动排除逻辑</h5><p>Broadcast、Server拉起，启动过程进入后台都需要排除统计。</p>
<h5 id="4、使用什么指标来衡量启动速度的快慢？"><a href="#4、使用什么指标来衡量启动速度的快慢？" class="headerlink" title="4、使用什么指标来衡量启动速度的快慢？"></a>4、使用什么指标来衡量启动速度的快慢？</h5><p><strong>平均启动时间的问题</strong></p>
<p>一些体验很差的用户很可能被平均了。</p>
<p><strong>建议的指标</strong></p>
<p>1、快开慢开比</p>
<p>如2s快开比，5s慢开比，可以看到有多少比例的用户体验好，多少比例的用户比较糟糕。</p>
<p>2、90%用户的启动时间</p>
<p>如果90%用户的启动时间都小于5s，那么90%区间的启动耗时就是5s。</p>
<h5 id="5、启动的类型有哪几种？"><a href="#5、启动的类型有哪几种？" class="headerlink" title="5、启动的类型有哪几种？"></a>5、启动的类型有哪几种？</h5><ul>
<li>首次安装启动</li>
<li>覆盖安装启动</li>
<li>冷启动（指标）</li>
<li>热启动（反映程序的活跃或保活能力）</li>
</ul>
<p>借鉴Facebook的profilo工具原理，对启动整个流程的耗时监控，在后台对不同的版本做自动化对比，监控新版本是否有新增耗时的函数。</p>
<h2 id="四、启动优化常规方案"><a href="#四、启动优化常规方案" class="headerlink" title="四、启动优化常规方案"></a>四、启动优化常规方案</h2><h3 id="启动过程中的常见问题"><a href="#启动过程中的常见问题" class="headerlink" title="启动过程中的常见问题"></a>启动过程中的常见问题</h3><ul>
<li>点击图标很久都不响应：预览窗口被禁用或设置为透明。</li>
<li>首页显示太慢：初始化任务太多。</li>
<li>首页显示后无法进行操作：太多延迟初始化任务占用主线程CPU时间片。</li>
</ul>
<h3 id="优化区域"><a href="#优化区域" class="headerlink" title="优化区域"></a>优化区域</h3><p>Application、Activity创建以及回调等过程。</p>
<h3 id="1、主题切换"><a href="#1、主题切换" class="headerlink" title="1、主题切换"></a>1、主题切换</h3><p>使用Activity的windowBackground主题属性预先设置一个启动图片（layer-list），在启动后，在Activity的onCreate()方法中的super.onCreate()前再setTheme(R.style.AppTheme)。</p>
<h4 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h4><ul>
<li>使用简单。</li>
<li>避免了启动白屏和点击启动图标不响应的情况。</li>
</ul>
<h4 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h4><ul>
<li>治标不治本，表面上产生一种快的感觉。</li>
<li>对于中低端机，总的闪屏时间会更长，建议只在Android6.0/7.0以上才启用“预览闪屏”方案，让手机性能好的用户可以有更好的体验。</li>
</ul>
<h3 id="2、第三方库懒加载"><a href="#2、第三方库懒加载" class="headerlink" title="2、第三方库懒加载"></a>2、第三方库懒加载</h3><p>按需初始化，如图片库的初始化等等。</p>
<h3 id="3、异步初始化预备知识-线程优化"><a href="#3、异步初始化预备知识-线程优化" class="headerlink" title="3、异步初始化预备知识-线程优化"></a>3、异步初始化预备知识-线程优化</h3><h4 id="1、Android线程调度原理剖析"><a href="#1、Android线程调度原理剖析" class="headerlink" title="1、Android线程调度原理剖析"></a>1、Android线程调度原理剖析</h4><h5 id="线程调度原理"><a href="#线程调度原理" class="headerlink" title="线程调度原理"></a>线程调度原理</h5><ul>
<li>任意时刻，只有一个线程占用CPU，处于运行状态。</li>
<li>多线程并发，轮流获取CPU使用权。</li>
<li>JVM负责线程调度，按照特定机制分配CPU使用权。</li>
</ul>
<h5 id="线程调度模型"><a href="#线程调度模型" class="headerlink" title="线程调度模型"></a>线程调度模型</h5><p><strong>1、分时调度模型</strong></p>
<p>轮流获取、均分CPU。</p>
<p><strong>2、抢占式调度模型</strong></p>
<p>优先级高的获取。</p>
<h5 id="如何干预线程调度？"><a href="#如何干预线程调度？" class="headerlink" title="如何干预线程调度？"></a>如何干预线程调度？</h5><p>设置线程优先级。</p>
<h5 id="Android线程调度"><a href="#Android线程调度" class="headerlink" title="Android线程调度"></a>Android线程调度</h5><p><strong>1、nice值</strong></p>
<ul>
<li>Process中定义。</li>
<li>值越小，优先级越高。</li>
<li>默认是THREAD_PRIORITY_DEFAUT，0。</li>
</ul>
<p><strong>2、cgroup</strong></p>
<ul>
<li>更严格的群组调度策略。</li>
<li>后台group。</li>
<li>前台group，保证前台线程可以获取到更多的CPU</li>
</ul>
<h5 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h5><ul>
<li>线程过多会导致CPU频繁切换，降低线程运行效率。</li>
<li>正确认识任务重要性决定哪种优先级。</li>
<li>优先级具有继承性。</li>
</ul>
<h4 id="2、Android异步方式"><a href="#2、Android异步方式" class="headerlink" title="2、Android异步方式"></a>2、Android异步方式</h4><h5 id="1、Thread"><a href="#1、Thread" class="headerlink" title="1、Thread"></a>1、Thread</h5><ul>
<li>最简单、常见的异步方式。</li>
<li>不易复用，频繁创建及销毁开销大。</li>
<li>复杂场景不易使用。</li>
</ul>
<h5 id="2、HandlerThread"><a href="#2、HandlerThread" class="headerlink" title="2、HandlerThread"></a>2、HandlerThread</h5><ul>
<li>自带消息循环的线程。</li>
<li>串行执行。</li>
<li>长时间运行，不断从队列中获取任务。</li>
</ul>
<h5 id="3、IntentService"><a href="#3、IntentService" class="headerlink" title="3、IntentService"></a>3、IntentService</h5><ul>
<li>继承自Service在内部创建HandlerThread。</li>
<li>异步，不占用主线程。</li>
<li>优先级较高，不易被系统Kill。</li>
</ul>
<h5 id="4、AsyncTask"><a href="#4、AsyncTask" class="headerlink" title="4、AsyncTask"></a>4、AsyncTask</h5><ul>
<li>Android提供的工具类。</li>
<li>无需自己处理线程切换。</li>
<li>需注意版本不一致问题（API 14以上解决）</li>
</ul>
<h5 id="5、线程池"><a href="#5、线程池" class="headerlink" title="5、线程池"></a>5、线程池</h5><ul>
<li>Java提供的线程池。</li>
<li>易复用，减少频繁创建、销毁的时间。</li>
<li>功能强大，如定时、任务队列、并发数控制等。</li>
</ul>
<h5 id="6、RxJava"><a href="#6、RxJava" class="headerlink" title="6、RxJava"></a>6、RxJava</h5><p>由强大的Scheduler集合提供。</p>
<p>不同类型的Scheduler：</p>
<ul>
<li>IO</li>
<li>Computation</li>
</ul>
<h5 id="异步方式总结"><a href="#异步方式总结" class="headerlink" title="异步方式总结"></a>异步方式总结</h5><ul>
<li>推荐度：从后往前排列。</li>
<li>正确场景选择正确的方式。</li>
</ul>
<h4 id="3、Android线程优化实战"><a href="#3、Android线程优化实战" class="headerlink" title="3、Android线程优化实战"></a>3、Android线程优化实战</h4><h5 id="线程使用准则"><a href="#线程使用准则" class="headerlink" title="线程使用准则"></a>线程使用准则</h5><ul>
<li>1、严禁使用new Thread方式。</li>
<li>2、提供基础线程池供各个业务线使用，避免各个业务线各自维护一套线程池，导致线程数过多。</li>
<li>3、根据任务类型选择合适的异步方式：优先级低，长时间执行，HandlerThread；定时执行耗时任务，线程池。</li>
<li>4、创建线程必须命名，以方便定位线程归属，在运行期Thread.currentThread().setName修改名字。</li>
<li>5、关键异步任务监控，注意异步不等于不耗时，建议使用AOP的方式来做监控。</li>
<li>6、重视优先级设置（根据任务具体情况），Process.setThreadPriority();可以设置多次。</li>
</ul>
<h4 id="4、如何锁定线程创建者"><a href="#4、如何锁定线程创建者" class="headerlink" title="4、如何锁定线程创建者"></a>4、如何锁定线程创建者</h4><h5 id="锁定线程创建背景"><a href="#锁定线程创建背景" class="headerlink" title="锁定线程创建背景"></a>锁定线程创建背景</h5><ul>
<li>项目变大之后收敛线程。</li>
<li>项目源码、三方库、aar中都有线程的创建。</li>
</ul>
<h5 id="锁定线程创建方案"><a href="#锁定线程创建方案" class="headerlink" title="锁定线程创建方案"></a>锁定线程创建方案</h5><p>特别适合Hook手段，找Hook点：构造函数或者特定方法，如Thread的构造函数。</p>
<h5 id="实战"><a href="#实战" class="headerlink" title="实战"></a>实战</h5><p>在attachBaseContext中调用DexposedBridge.hookAllConstructors方法，如下所示：</p>
<pre><code>DexposedBridge.hookAllConstructors(Thread.class, new XC_MethodHook() { 
    @Override protected void afterHookedMethod（MethodHookParam param）throws Throwable {                         
        super.afterHookedMethod(param); 
        Thread thread = (Thread) param.thisObject; 
        LogUtils.i("stack " + Log.getStackTraceString(new Throwable());
    }
);
</code></pre><p>从log找到线程创建信息，根据堆栈信息跟相关业务方沟通解决方案。</p>
<h4 id="5、线程收敛优雅实践初步"><a href="#5、线程收敛优雅实践初步" class="headerlink" title="5、线程收敛优雅实践初步"></a>5、线程收敛优雅实践初步</h4><h5 id="线程收敛常规方案"><a href="#线程收敛常规方案" class="headerlink" title="线程收敛常规方案"></a>线程收敛常规方案</h5><ul>
<li>根据线程创建堆栈考量合理性，使用同一线程库。</li>
<li>各业务线下掉自己的线程库。</li>
</ul>
<h5 id="问题：基础库怎么使用线程？"><a href="#问题：基础库怎么使用线程？" class="headerlink" title="问题：基础库怎么使用线程？"></a>问题：基础库怎么使用线程？</h5><p>直接依赖线程库。</p>
<p><strong>缺点：</strong></p>
<ul>
<li>线程库更新可能会导致基础库更新。</li>
</ul>
<h5 id="基础库优雅使用线程"><a href="#基础库优雅使用线程" class="headerlink" title="基础库优雅使用线程"></a>基础库优雅使用线程</h5><ul>
<li>基础库内部暴露API：setExecutor。</li>
<li>初始化的时候注入统一的线程库。</li>
</ul>
<h5 id="统一线程库时区分任务类型"><a href="#统一线程库时区分任务类型" class="headerlink" title="统一线程库时区分任务类型"></a>统一线程库时区分任务类型</h5><ul>
<li>IO密集型：IO密集型任务不消耗CPU，核心池可以很大。</li>
<li>CPU密集型：核心池大小和CPU核心数相关。</li>
</ul>
<h4 id="6、线程优化核心问题"><a href="#6、线程优化核心问题" class="headerlink" title="6、线程优化核心问题"></a>6、线程优化核心问题</h4><h5 id="1、线程使用为什么会遇到问题？"><a href="#1、线程使用为什么会遇到问题？" class="headerlink" title="1、线程使用为什么会遇到问题？"></a>1、线程使用为什么会遇到问题？</h5><p>项目发展阶段忽视基础设施建设，没有采用统一的线程池，导致线程数量过多。</p>
<p><strong>表现形式：</strong></p>
<p>异步任务执行太耗时，导致主线程卡顿。</p>
<p><strong>问题原因：</strong></p>
<ul>
<li>Java线程调度是抢占式的，线程优先级比较重要，需要区分。</li>
<li>没有区分IO和CPU密集型任务，导致主线程抢不到CPU。</li>
</ul>
<h5 id="2、怎么在项目中对线程进行优化？"><a href="#2、怎么在项目中对线程进行优化？" class="headerlink" title="2、怎么在项目中对线程进行优化？"></a>2、怎么在项目中对线程进行优化？</h5><p><strong>核心：线程收敛</strong></p>
<ul>
<li>通过Hook方式找到对应线程的堆栈信息，和业务方讨论是否应该单独起一个线程，尽可能使用统一线程池。</li>
<li>每个基础库都暴露一个设置线程池的方法，以避免线程库更新导致基础库需要更新的问题。</li>
<li>统一线程池应注意IO、CPU密集型任务区分</li>
<li>其它细节：重要异步任务统计耗时、注重异步任务优先级和线程名的设置。</li>
</ul>
<h3 id="4、异步初始化"><a href="#4、异步初始化" class="headerlink" title="4、异步初始化"></a>4、异步初始化</h3><h4 id="1、核心思想"><a href="#1、核心思想" class="headerlink" title="1、核心思想"></a>1、核心思想</h4><p>子线程分担主线程任务，并行减少时间。</p>
<h4 id="2、异步优化注意点"><a href="#2、异步优化注意点" class="headerlink" title="2、异步优化注意点"></a>2、异步优化注意点</h4><ul>
<li>1、不符合异步要求。</li>
<li>2、需要在某个阶段完成（采用CountDownLatch确保异步任务完成后才到下一个阶段）。</li>
<li>3、如出现主线程要使用时还没初始化则在此次使用前初始化。</li>
<li>4、区分CPU密集型和IO密集型任务。</li>
</ul>
<h4 id="3、异步初始化方案演进"><a href="#3、异步初始化方案演进" class="headerlink" title="3、异步初始化方案演进"></a>3、异步初始化方案演进</h4><ul>
<li>1、new Thread</li>
<li>2、IntentService</li>
<li>3、线程池（合理配置并选择CPU密集型和IO密集型线程池）</li>
<li>4、异步启动器</li>
</ul>
<h4 id="4、异步优化最优解：异步启动器"><a href="#4、异步优化最优解：异步启动器" class="headerlink" title="4、异步优化最优解：异步启动器"></a>4、异步优化最优解：异步启动器</h4><h5 id="常规异步优化痛点："><a href="#常规异步优化痛点：" class="headerlink" title="常规异步优化痛点："></a>常规异步优化痛点：</h5><ul>
<li>1、代码不优雅。</li>
<li>2、场景不好处理（依赖关系）。</li>
<li>3、维护成本高。</li>
</ul>
<h5 id="核心思想："><a href="#核心思想：" class="headerlink" title="核心思想："></a>核心思想：</h5><p>充分利用CPU多核，自动梳理任务顺序。</p>
<h5 id="启动器流程"><a href="#启动器流程" class="headerlink" title="启动器流程"></a>启动器流程</h5><ul>
<li>任务Task化，启动逻辑抽象成Task。</li>
<li>根据所有任务依赖关系排序生成一个有向无环图。</li>
<li>多线程按照排序后的优先级依次执行。</li>
</ul>
<h3 id="5、延迟初始化"><a href="#5、延迟初始化" class="headerlink" title="5、延迟初始化"></a>5、延迟初始化</h3><h4 id="1、常规方案：利用闪屏页的停留时间进行部分初始化"><a href="#1、常规方案：利用闪屏页的停留时间进行部分初始化" class="headerlink" title="1、常规方案：利用闪屏页的停留时间进行部分初始化"></a>1、常规方案：利用闪屏页的停留时间进行部分初始化</h4><ul>
<li>new Handler().postDelayed()。</li>
<li>界面UI展示后调用。</li>
</ul>
<h4 id="2、常规初始化痛点"><a href="#2、常规初始化痛点" class="headerlink" title="2、常规初始化痛点"></a>2、常规初始化痛点</h4><ul>
<li>时机不容易控制。</li>
<li>导致界面UI卡顿。</li>
</ul>
<h4 id="3、延迟优化最优解：延迟启动器"><a href="#3、延迟优化最优解：延迟启动器" class="headerlink" title="3、延迟优化最优解：延迟启动器"></a>3、延迟优化最优解：延迟启动器</h4><h5 id="核心思想"><a href="#核心思想" class="headerlink" title="核心思想"></a>核心思想</h5><p>利用IdleHandler特性，在CPU空闲时执行，对延迟任务进行分批初始化。</p>
<h5 id="优势"><a href="#优势" class="headerlink" title="优势"></a>优势</h5><ul>
<li>执行时机明确。</li>
<li>缓解界面UI卡顿。</li>
<li>真正提升用户体验。</li>
</ul>
<h3 id="6、Multidex预加载优化"><a href="#6、Multidex预加载优化" class="headerlink" title="6、Multidex预加载优化"></a>6、Multidex预加载优化</h3><h4 id="1、优化步骤："><a href="#1、优化步骤：" class="headerlink" title="1、优化步骤："></a>1、优化步骤：</h4><ul>
<li>1、启动时单独开一个进程去异步进行Multidex的第一次加载，即Dex提取和Dexopt操作。</li>
<li>2、此时，主进程Application进入while循环，不断检测Multidex操作是否完成。</li>
<li>3、执行到Multidex时，则已经发现提取并优化好了Dex，直接执行。MultiDex执行完之后主进程Application继续执行ContentProvider初始化和Application的onCreate方法。</li>
</ul>
<p><strong>注意：</strong></p>
<p>5.0以上默认使用ART，在安装时已将Class.dex转换为oat文件了，无需优化，所以应判断只有在主进程及SDK 5.0以下才进行Multidex的预加载。</p>
<h4 id="2、dex-opt过程是怎样的？"><a href="#2、dex-opt过程是怎样的？" class="headerlink" title="2、dex-opt过程是怎样的？"></a>2、dex-opt过程是怎样的？</h4><p>主要包括inline以及quick指令的优化。</p>
<h5 id="inline是什么？"><a href="#inline是什么？" class="headerlink" title="inline是什么？"></a>inline是什么？</h5><p>使编译器在函数调用处用函数体代码代替函数调用指令。</p>
<h5 id="inline的作用？"><a href="#inline的作用？" class="headerlink" title="inline的作用？"></a>inline的作用？</h5><p>函数调用的转移操作有一定的时间和空间方面的开销，特别是对于一些函数体不大且频繁调用的函数，解决其效率问题更为重要，引入inline函数就是为了解决这一问题。</p>
<h5 id="inline是如何进行优化的？"><a href="#inline是如何进行优化的？" class="headerlink" title="inline是如何进行优化的？"></a>inline是如何进行优化的？</h5><p>inline函数至少在三个方面提升了程序的时间性能：</p>
<ul>
<li>1、避免了函数调用必须执行的压栈出栈等操作。</li>
<li>2、由于函数体代码被移到函数调用处，编译器可以获得更多的上下文信息，并根据这些信息对函数体代码和被调用者代码进行更进一步的优化。</li>
<li>3、若不使用inline函数，程序执行至函数调用处，需要转而去执行函数体所在位置的代码。一般函数调用位置和函数代码所在位置在代码段中并不相近，这样很容易形成操作系统的缺页中断。操作系统需要把缺页地址的代码从硬盘移入内存，所需时间将成数量级增加。而使用inline函数则可以减少缺页中断发生的机会。</li>
</ul>
<h5 id="inline使用时应注意的问题？"><a href="#inline使用时应注意的问题？" class="headerlink" title="inline使用时应注意的问题？"></a>inline使用时应注意的问题？</h5><ul>
<li>由于inline函数在函数调用处插入函数体代码代替函数调用，若该函数在程序的很多位置被调用，有可能造成内存空间的浪费。</li>
<li>一般程序的压栈出栈操作也需要一定的代码，这段代码完成栈指针调整、参数传递、现场保护和恢复等操作。<br>若函数的函数体代码量小于编译器生成的函数压栈出栈代码，则可以放心地定义为inline，这个时候占用内存空间反而会减小。而当函数体代码大于函数压栈出栈代码时，将函数定义为inline就会增加内存空间的使用。</li>
<li>C++程序应该根据应用的具体场景、函数体大小、调用位置多少、函数调用的频率、应用场景对时间性能的要求，应用场景对内存性能的要求等各方面因素合理决定是否定义inline函数。</li>
<li>inline函数内不允许用循环语句和开关语句。</li>
</ul>
<h5 id="3、预加载SharedPreferences"><a href="#3、预加载SharedPreferences" class="headerlink" title="3、预加载SharedPreferences"></a>3、预加载SharedPreferences</h5><p>可以利用MultiDex预加载期间的这段CPU去预加载SharedPreferences。</p>
<p><strong>注意：</strong></p>
<p>需重写getApplicationContext返回this，否则此时可能获取不到context。</p>
<h3 id="7、类预加载优化"><a href="#7、类预加载优化" class="headerlink" title="7、类预加载优化"></a>7、类预加载优化</h3><p>在Application中提前异步加载初始化耗时较长的类。</p>
<h4 id="如何找到耗时较长的类？"><a href="#如何找到耗时较长的类？" class="headerlink" title="如何找到耗时较长的类？"></a>如何找到耗时较长的类？</h4><p>替换系统的ClassLoader，打印类加载的时间，按需选取需要异步加载的类。</p>
<p><strong>注意：</strong></p>
<ul>
<li>Class.forName()只加载类本身及其静态变量的引用类。</li>
<li>new 类实例 可以额外加载类成员变量的引用类。</li>
</ul>
<h3 id="8、WebView启动优化"><a href="#8、WebView启动优化" class="headerlink" title="8、WebView启动优化"></a>8、WebView启动优化</h3><ul>
<li>1、WebView首次创建比较耗时，需要预先创建WebView提前将其内核初始化。</li>
<li>2、使用WebView缓存池，用到WebView的时候都从缓存池中拿，注意内存泄漏问题。</li>
<li>3、本地离线包，即预置静态页面资源。</li>
</ul>
<h3 id="9、页面数据预加载"><a href="#9、页面数据预加载" class="headerlink" title="9、页面数据预加载"></a>9、页面数据预加载</h3><p>在主页空闲时，将其它页面的数据加载好保存到内存或数据库，等到打开该页面时，判断已经预加载过，就直接从内存或数据库取数据并显示。</p>
<h3 id="10、启动阶段不启动子进程"><a href="#10、启动阶段不启动子进程" class="headerlink" title="10、启动阶段不启动子进程"></a>10、启动阶段不启动子进程</h3><p>子进程会共享CPU资源，导致主进程CPU紧张。此外，在多进程情况下一定要可以在onCreate中去区分进程做一些初始化工作。</p>
<p><strong>注意启动顺序：</strong></p>
<p>App onCreate之前是ContentProvider初始化。</p>
<h3 id="11、闪屏页与主页的绘制优化"><a href="#11、闪屏页与主页的绘制优化" class="headerlink" title="11、闪屏页与主页的绘制优化"></a>11、闪屏页与主页的绘制优化</h3><ul>
<li>1、布局优化。</li>
<li>2、过渡绘制优化。</li>
</ul>
<p>关于绘制优化可以参考<a href="https://jsonchao.github.io/2019/07/28/Android%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E7%BB%98%E5%88%B6%E4%BC%98%E5%8C%96/" target="_blank" rel="external">Android性能优化之绘制优化</a>。</p>
<h2 id="五、启动优化黑科技"><a href="#五、启动优化黑科技" class="headerlink" title="五、启动优化黑科技"></a>五、启动优化黑科技</h2><h3 id="1、启动阶段抑制GC"><a href="#1、启动阶段抑制GC" class="headerlink" title="1、启动阶段抑制GC"></a>1、启动阶段抑制GC</h3><p>启动时CG抑制，允许堆一直增长，直到手动或OOM停止GC抑制。（空间换时间）</p>
<h4 id="前提条件"><a href="#前提条件" class="headerlink" title="前提条件"></a>前提条件</h4><ul>
<li>1、设备厂商没有加密内存中的Dalvik库文件。</li>
<li>2、设备厂商没有改动Google的Dalvik源码。</li>
</ul>
<h4 id="实现原理"><a href="#实现原理" class="headerlink" title="实现原理"></a>实现原理</h4><ul>
<li>在源码级别找到抑制GC的修改方法，例如改变跳转分支。</li>
<li>在二进制代码里找到 A 分支条件跳转的”指令指纹”，以及用于改变分支的二进制代码，假设为 override_A。</li>
<li>应用启动后扫描内存中的 libdvm.so，根据”指令指纹”定位到修改位置，然后用 override_A 覆盖。</li>
</ul>
<h4 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h4><p>白名单覆盖所有设备，但维护成本高。</p>
<h3 id="2、CPU锁频"><a href="#2、CPU锁频" class="headerlink" title="2、CPU锁频"></a>2、CPU锁频</h3><p>在Android系统中，CPU相关的信息存储在/sys/devices/system/cpu目录的文件中，通过对该目录下的特定文件进行写值，实现对CPU频率等状态信息的更改。</p>
<h4 id="缺点-2"><a href="#缺点-2" class="headerlink" title="缺点"></a>缺点</h4><p>暴力拉伸CPU频率，导致耗电量增加。</p>
<h4 id="CPU工作模式"><a href="#CPU工作模式" class="headerlink" title="CPU工作模式"></a>CPU工作模式</h4><ul>
<li>performance：最高性能模式，即使系统负载非常低，cpu也在最高频率下运行。</li>
<li>powersave：省电模式，与performance模式相反，cpu始终在最低频率下运行。</li>
<li>ondemand：CPU频率跟随系统负载进行变化。</li>
<li>userspace：可以简单理解为自定义模式，在该模式下可以对频率进行设定。</li>
</ul>
<h4 id="CPU的工作频率范围"><a href="#CPU的工作频率范围" class="headerlink" title="CPU的工作频率范围"></a>CPU的工作频率范围</h4><p>对应的文件有：</p>
<ul>
<li>cpuinfo_max_freq</li>
<li>cpuinfo_min_freq</li>
<li>scaling_max_freq</li>
<li>scaling_min_freq</li>
</ul>
<h3 id="3、IO优化"><a href="#3、IO优化" class="headerlink" title="3、IO优化"></a>3、IO优化</h3><ul>
<li>1、启动过程不建议出现网络IO。</li>
<li>2、为了只解析启动过程中用到的数据，应选择合适的数据结构，如将ArrayMap改造成支持随机读写、延时解析的数据存储结构以替代SharePreference。</li>
</ul>
<p>注意：考虑重度用户的使用场景。</p>
<h4 id="Linux-IO知识补充"><a href="#Linux-IO知识补充" class="headerlink" title="Linux IO知识补充"></a>Linux IO知识补充</h4><h5 id="1、磁盘高速缓存技术"><a href="#1、磁盘高速缓存技术" class="headerlink" title="1、磁盘高速缓存技术"></a>1、磁盘高速缓存技术</h5><p>利用内存中的存储空间来暂存从磁盘中读出的一系列盘块中的信息。因此，磁盘高速缓存在逻辑上属于磁盘，物理上则是驻留在内存中的盘块。</p>
<p>其内存中分为两种形式：</p>
<ul>
<li>在内存中开辟一个单独的存储空间作为磁速缓存，大小固定。</li>
<li>把未利用的内存空间作为一个缓沖池，供请求分页系统和磁盘I/O时共享。</li>
</ul>
<h5 id="2、分页"><a href="#2、分页" class="headerlink" title="2、分页"></a>2、分页</h5><ul>
<li>存储器管理的一种技术。</li>
<li>可以使电脑的主存使用存储在辅助存储器中的数据。</li>
<li>操作系统会将辅助存储器（通常是磁盘）中的数据分区成固定大小的区块，称为“页”（pages）。<br>当不需要时，将分页由主存（通常是内存）移到辅助存储器；当需要时，再将数据取回，加载主存中。</li>
<li>相对于分段，分页允许存储器存储于不连续的区块以维持文件系统的整齐。</li>
<li>分页是磁盘和内存间传输数据块的最小单位。</li>
</ul>
<h5 id="3、高速缓存-缓冲器"><a href="#3、高速缓存-缓冲器" class="headerlink" title="3、高速缓存/缓冲器"></a>3、高速缓存/缓冲器</h5><ul>
<li>都是介于高速设备和低速设备之间。</li>
<li>高速缓存存放的是低速设备中某些数据的复制数据，而缓冲器则可同时存储高低速设备之间的数据。</li>
<li>高速缓存存放的是高速设备经常要访问的数据。</li>
</ul>
<h5 id="4、linux同步IO：sync、fsync、fdatasync"><a href="#4、linux同步IO：sync、fsync、fdatasync" class="headerlink" title="4、linux同步IO：sync、fsync、fdatasync"></a>4、linux同步IO：sync、fsync、fdatasync</h5><p><strong>为什么要使用同步IO？</strong></p>
<p>当数据写入文件时，内核通常先将该数据复制到缓冲区高速缓存或页面缓存中，如果该缓冲区尚未写满，则不会将其排入输入队列，而是等待其写满或内核需要重用该缓冲区以便存放其他磁盘块数据时，再将该缓冲排入输出队列，最后等待其到达队首时，才进行实际的IO操作—延迟写。</p>
<p>延迟写减少了磁盘读写次数，但是却降低了文件内容的更新速度，可能会造成文件更新内容的丢失。为了保证数据一致性，则需使用同步IO。</p>
<p><strong>sync</strong></p>
<ul>
<li>sync函数只是将所有修改过的块缓冲区排入写队列，然后就返回，它并不等待实际磁盘写操作结束再返回。</li>
<li>通常称为update的系统守护进程会周期性地（一般每隔30秒）调用sync函数。这就保证了定期冲洗内核的块缓冲区。</li>
</ul>
<p><strong>fsync</strong></p>
<ul>
<li>fsync函数只对文件描述符filedes指定的单一文件起作用，并且等待磁盘IO写结束后再返回。通常应用于需要确保将修改内容立即写到磁盘的应用如数据库。</li>
<li>文件的数据和metadata通常存放在硬盘的不同地方，因此fsync至少需要两次IO操作。</li>
</ul>
<p><strong>补充：msync</strong></p>
<p>如果当前硬盘的平均寻道时间是3-15ms，7200RPM硬盘的平均旋转延迟大约为4ms，因此一次IO操作的耗时大约为10ms。</p>
<p>如果使用内存映射文件的方式进行文件IO（mmap），将文件的page cache直接映射到进程的地址空间，这时需要使用msync系统调用确保修改的内容完全同步到硬盘之上。</p>
<p><strong>fdatasync</strong></p>
<ul>
<li>fdatasync函数类似于fsync，但它只影响文件的数据部分。而fsync还会同步更新文件的属性。</li>
<li>仅仅只在必要（如文件尺寸需要立即同步）的情况下才会同步metadata，因此可以减少一次IO操作。</li>
</ul>
<p><strong>日志文件都是追加性的，文件尺寸一致在增大，如何利用好fdatasync减少日志文件的同步开销？</strong></p>
<p>创建每个log文件时先写文件的最后一个page，将log文件扩展为10MB大小，这样便可以使用fdatasync，每写10MB只有一次同步metadata的开销。</p>
<h5 id="5、网络IO和磁盘IO"><a href="#5、网络IO和磁盘IO" class="headerlink" title="5、网络IO和磁盘IO"></a>5、网络IO和磁盘IO</h5><p><strong>磁盘IO（缓存IO）</strong></p>
<p>标准IO，大多数文件系统默认的IO操作。</p>
<ul>
<li>数据先从磁盘复制到内核空间的缓冲区，然后再从内核空间中的缓冲区复制到应用程序的缓冲区。</li>
<li>读操作：操作系统检查内核的缓冲区有没有需要的数据，如果已经有缓存了，那么直接从缓存中返回；否则，从磁盘中返回，再缓存在操作系统的磁盘中。</li>
<li>写操作：将数据从用户空间复制到内核空间中的缓冲区中，这时对用户来说写操作就已经完成，至于什么时候写到磁盘中，由操作系统决定，除非显示地调用了sync同步命令。</li>
</ul>
<p><strong>优点</strong></p>
<ul>
<li>在一定程度上分离了内核空间和用户空间，保护系统本身安全。</li>
<li>可以减少磁盘IO的读写次数，从而提高性能。</li>
</ul>
<p><strong>缺点</strong></p>
<p>DMA方式可以将数据直接从磁盘读到页缓存中，或者将数据从页缓存中写回到磁盘，而不能在应用程序地址空间和磁盘之间进行数据传输，这样，数据在传输过程中需要在应用程序地址空间（用户空间）和缓存（内核空间）中进行多次数据拷贝操作，这带来的CPU以及内存开销是非常大的。</p>
<p><strong>磁盘IO主要的延时（15000RPM硬盘为例）</strong></p>
<p>机械转动延时（平均2ms）+ 寻址延时（2~3ms）+ 块传输延时（0.1ms左右）=&gt; 平均5ms</p>
<p><strong>网络IO主要延时</strong></p>
<p>服务器响应延时 + 带宽限制 + 网络延时 + 跳转路由延时 + 本地接收延时（一般为几十毫秒到几千毫秒，受环境影响极大）</p>
<h5 id="6、PIO与DMA"><a href="#6、PIO与DMA" class="headerlink" title="6、PIO与DMA"></a>6、PIO与DMA</h5><p><strong>PIO</strong></p>
<p>很早之前，磁盘和内存之间的数据传输是需要CPU控制的，也就是读取磁盘文件到内存中时，数据会经过CPU存储转发，这种方式称为PIO。</p>
<p><strong>DMA（直接内存访问，Direct Memory Access）</strong></p>
<ul>
<li>可以不经过CPU而直接进行磁盘和内存的数据交换。</li>
<li>CPU只需要向DMA控制器下达指令，让DMA控制器来处理数据的传送即可。</li>
<li>DMA控制器通过系统总线来传输数据，传送完毕再通知CPU，这样就在很大程度上降低了CPU占用率，大大节省了系统资源，而它的传输速度与PIO的差异并不明显，而这主要取决于慢速设备的速度。</li>
</ul>
<h5 id="7、直接IO与异步IO"><a href="#7、直接IO与异步IO" class="headerlink" title="7、直接IO与异步IO"></a>7、直接IO与异步IO</h5><p><strong>直接IO</strong></p>
<p>应用程序直接访问磁盘数据，而不经过内核缓冲区。以减少从内核缓冲区到用户数据缓存的数据复制。</p>
<p><strong>异步IO</strong></p>
<p>当访问数据的线程发出请求后，线程会接着去处理其它事情，而不是阻塞等待。</p>
<h5 id="8、VFS（虚拟文件系统，Virtual-File-System）"><a href="#8、VFS（虚拟文件系统，Virtual-File-System）" class="headerlink" title="8、VFS（虚拟文件系统，Virtual File System）"></a>8、VFS（虚拟文件系统，Virtual File System）</h5><p>可以为访问文件系统的系统调用提供一个统一的抽象接口。</p>
<h3 id="4、数据重排"><a href="#4、数据重排" class="headerlink" title="4、数据重排"></a>4、数据重排</h3><p>Dex文件用到的类和APK里面各种资源文件都比较小，读取频繁，且磁盘地址分布范围比较广。我们可以利用Linux文件IO流程中的page cache机制将它们按照读取顺序重新排列在一起，以减少真实的磁盘IO次数。</p>
<h4 id="1、类重排"><a href="#1、类重排" class="headerlink" title="1、类重排"></a>1、类重排</h4><p>使用Facebook的ReDex的Interdex调整类在Dex中的排列顺序。</p>
<h4 id="2、资源文件重排"><a href="#2、资源文件重排" class="headerlink" title="2、资源文件重排"></a>2、资源文件重排</h4><ul>
<li>最佳方案是修改内核源码，实现统计、度量、自动化。</li>
<li>其次可以使用Hook框架进行统计得出资源加载顺序列表。</li>
<li>最后，调整apk文件列表需要修改7zip源码以支持传入文件列表顺序。</li>
</ul>
<h4 id="技术视野："><a href="#技术视野：" class="headerlink" title="技术视野："></a>技术视野：</h4><ul>
<li>所谓的创新，不一定是要创造前所未有的东西，也可以将已有的方案移植到新的平台，并结合该平台的特性落地，就是一个很大的创新。</li>
<li>当我们足够熟悉底层的知识时，可以利用系统的特性去做更加深层次的优化。</li>
</ul>
<h4 id="3、Hook框架"><a href="#3、Hook框架" class="headerlink" title="3、Hook框架"></a>3、Hook框架</h4><h5 id="Xposed框架是什么？"><a href="#Xposed框架是什么？" class="headerlink" title="Xposed框架是什么？"></a>Xposed框架是什么？</h5><p>一个可以不修改APK就影响程序运行的Hook框架。</p>
<h5 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h5><p>用自身实现的app_process替换掉系统/system/bin/app_process，加载一个额外的XposedBridge的jar包，用于将入口osZygoteInit.main()替换成XposedBridge.main()。之后，创建的Zygote进程和其子进程都是Hook过的了。</p>
<p>使用具体细节参见<a href="https://blog.csdn.net/coder_pig/article/details/80031285" target="_blank" rel="external">Xposed教程</a>。</p>
<h3 id="5、类加载优化（Dalvik）"><a href="#5、类加载优化（Dalvik）" class="headerlink" title="5、类加载优化（Dalvik）"></a>5、类加载优化（Dalvik）</h3><h4 id="1、类预加载原理"><a href="#1、类预加载原理" class="headerlink" title="1、类预加载原理"></a>1、类预加载原理</h4><p>对象第一次创建的时候，JVM首先检查对应的Class对象是否已经加载。如果没有加载，JVM会根据类名查找.class文件，将其Class对象载入。同一个类第二次new的时候就不需要加载类对象，而是直接实例化，创建时间就缩短了。</p>
<h4 id="2、类加载优化过程"><a href="#2、类加载优化过程" class="headerlink" title="2、类加载优化过程"></a>2、类加载优化过程</h4><ul>
<li>在Dalvik VM加载类的时候会有一个类校验过程，它需要校验方法的每一个指令。</li>
<li>通过Hook去掉verify步骤 -&gt; 几十ms的优化</li>
<li>最大优化场景在于首次安装和覆盖安装时，在Dalvik平台上，一个2MB的Dex正常需要350ms，将classVerifyMode设为VERIFY_MODE_NONE后，只需150ms，节省超过50%时间。</li>
</ul>
<p>ART比较复杂，Hook需要兼容几个版本。而且在安装时，大部分Dex已经优化好了，去掉ART平台的verify只会对动态加载的Dex带来一些好处。所以暂时不建议在ART平台使用。</p>
<h4 id="3、延伸：插件化和热修复"><a href="#3、延伸：插件化和热修复" class="headerlink" title="3、延伸：插件化和热修复"></a>3、延伸：插件化和热修复</h4><p>在设计上都存在大量的Hook和私有API调用</p>
<p><strong>缺点：</strong></p>
<p>1、稳定性较差</p>
<p>由于厂商的兼容性、安装失败、ART加载时dex2oat失败等原因，还是会有一些代码和资源的异常。Android P推出的non-sdk-interface调用限制，以后适配只会越来越难，成本越来越高。</p>
<p>2、性能问题</p>
<p>用到一些黑科技导致底层Runtime的优化享受不到。如Tinker加载补丁后，启动速度会降低5%~10%。</p>
<h5 id="1、各项热补丁技术的优缺点"><a href="#1、各项热补丁技术的优缺点" class="headerlink" title="1、各项热补丁技术的优缺点"></a>1、各项热补丁技术的优缺点</h5><p><strong>缺点：</strong></p>
<ul>
<li>只针对单一客户端版本，随着版本差异变大补丁体积也会变大。</li>
<li>不支持所有修改，如AndroidManifest。</li>
<li>对代码和资源的更新成功率无法达到100%。</li>
</ul>
<p><strong>优点：</strong></p>
<ul>
<li>降低开发成本，轻量而快速地升级。发布补丁等同于发布版本，也应该完整地执行测试与上线流程。</li>
<li>远端调试，只为特定用户发送补丁。</li>
<li>数据统计，对同一批用户更换补丁版本，能够更好地进行ABTest，得到更精确的数据。</li>
</ul>
<h5 id="2、InstanceRun实现机制"><a href="#2、InstanceRun实现机制" class="headerlink" title="2、InstanceRun实现机制"></a>2、InstanceRun实现机制</h5><p>Android官方使用热补丁技术实现InstantRun。</p>
<p><strong>应用构建流程：</strong></p>
<p>构建 -&gt; 部署 -&gt; 安装 -&gt; 重启app -&gt; 重启activity</p>
<p><strong>实现目标：</strong></p>
<p>尽可能多的剔除不必要的步骤，然后提升必要步骤的速度。</p>
<p><strong>InstantRun构建的三种方式：</strong></p>
<p><strong>1、HotSwap</strong></p>
<p>增量构建 -&gt; 改变部署</p>
<p><strong>场景：</strong></p>
<p>适用于多数简单的改变（包括一些方法实现的修改，或者变量值修改）。</p>
<p><strong>2、Warm Swap</strong></p>
<p>增量构建 -&gt; 改变部署 -&gt; activity重启</p>
<p><strong>场景：</strong></p>
<p>一般是修改了resources。</p>
<p><strong>3、Cold Swap</strong></p>
<p>增量构建 -&gt; 改变部署 -&gt; 应用重启 -&gt; activity重启</p>
<p><strong>场景：</strong></p>
<p>涉及结构性变化，如修改了继承规则或方法签名。</p>
<p><strong>补充：apk打包流程</strong></p>
<p>manifest文件合并、打包，和res一起被AAPT合并到APK中，同时项目代码被编译成字节码，然后转换成.dex文件，也被合并到APK中。</p>
<p><strong>Android打包流程回顾，最后对于release签名apk需要进行zipalign优化，它是指什么？</strong></p>
<p>概念补充：内存对齐（DSA，Data Structure Alignment）</p>
<p>各种类型的数据按照一定的规则在内存空间上排列，这就是对齐。</p>
<p><strong>优势：</strong></p>
<p>基于效率考虑，空间换时间，减少数据存取指令周期。</p>
<p><strong>编译器内存字节对齐的原则是什么？</strong></p>
<ul>
<li>1、数据类型的自身对齐值就是其长度（64位 OS）。</li>
<li>2、结构体或类的自身对齐值就是成员中自身对齐值最大的那个。需要起始地址必须是其相应有效对齐值的整数，并要求结构体的大小也为该结构体有效对齐值的整数倍。</li>
</ul>
<p>zipalign优化的最根本目的是<strong>帮助操作系统更高效地根据请求索引资源，使用resource-handling code统一将DSA限定为4byte。</strong></p>
<p><strong>手动执行Align优化</strong></p>
<p>利用build-tools文件夹下对应Android版本中的zipalign工具：</p>
<pre><code>zipalign -v 4 source.apk androidres.apk
</code></pre><p>检查当前APK是否已经执行过Align优化：</p>
<pre><code>zipalign -c -v 4 androidres.apk
</code></pre><p>其中：</p>
<ul>
<li>-c：检查。</li>
<li>-v：代表详细输出。</li>
<li>4：代表对齐为4个字节。</li>
</ul>
<p><strong>首次运行Instant Run，Gradle执行的操作</strong></p>
<ul>
<li>在有Instant Run的环境下：一个新的App Server会被注入到App中，与Bytecode instrumentation协同监控代码的变化。</li>
<li>同时会有一个新的Application类，它注入了一个自定义类加载器。同时该Application会启动我们所需的新注入的App Server。于是，AndroidManifest会被修改来确保我们能使用这个新的Application。</li>
<li>使用的时候，它会通过决策，合理运用冷温热拔插来协助我们大量地缩短构建程序的时间。</li>
</ul>
<p><strong>HotSwap原理</strong></p>
<p>Android Studio monitors<br>运行着Gradle任务来生成增量.dex文件（dex对应着开发中的修改类），AS会提取这些.dex文件发送到App Server，然后部署到App。因为原来版本的类都装载在运行中的程序了，Gradle会解释更新好这些.dex文件，发送到App Server的时候，交给自定义的类加载器来加载.dex文件。<br>App Server会不断地监听是否需要重写类文件，如果需要，任务会被立马执行，新的更改便能立即被响应。</p>
<p><strong>注意：</strong></p>
<p>此时Instant Run是不能回退的，必须重启应用响应修改。</p>
<p><strong>WarmSwap原理</strong></p>
<p>因为资源文件是在Activity创建时加载，所以必须重启Activity加载资源文件。</p>
<p><strong>注意：</strong></p>
<p>AndroidManifest的值是在APK安装的时候被读取的，所以需要触发一个完整的应用构建和部署。</p>
<p>ColdSwap原理</p>
<p>应用部署的时候，会把工程拆分成十个部分，每个部分都拥有自己的.dex文件，然后所有的类会根据包名被分配给相应的.dex文件。当ColdSwap开启时，修改过的类所对应的的.dex文件，会重组生成新的.dex文件，然后再部署到设备上。</p>
<p><strong>注意：</strong></p>
<p>应用多进程会被降级为ColdSwap。</p>
<h5 id="3、AndFix"><a href="#3、AndFix" class="headerlink" title="3、AndFix"></a>3、AndFix</h5><p><strong>实现原理：</strong></p>
<p>native hook -&gt; dalvik_repleaceMethod -&gt; 无法支持新增或删除filed的情况 -&gt; 需修复特定问题</p>
<p><strong>优点：</strong></p>
<ul>
<li>立即生效</li>
<li>补丁较小</li>
</ul>
<p><strong>缺点：</strong></p>
<ul>
<li>兼容性不佳</li>
<li>开发不透明</li>
</ul>
<h5 id="4、Qzone"><a href="#4、Qzone" class="headerlink" title="4、Qzone"></a>4、Qzone</h5><p>基于Android Dex分包方案。</p>
<p>将多个dex文件放入到app的classloader中，但是android dex拆包方案中的类是没有重复的，如果classes.dex和classes1.dex中有重复的类，<strong>当用到这个重复的类时，系统会选择哪个类进行加载呢？</strong></p>
<p>一个ClassLoader可以包含多个dex文件，每个dex文件是一个Elements，多个dex文件排列成有序的dexElements，当找类的时候，会按顺序遍历dex文件，然后从当前遍历的dex文件中找类，如果找到则返回，如果找不到从下一个dex文件继续查找。</p>
<p>所以，如果在不同的dex中有相同的类存在，那么会优先选择排在前面的dex文件的类。</p>
<p>Qzone热补丁方案就是把有问题的类打包到一个dex（patch.dex）中去，然后把这个dex插入到Elements的最前面。</p>
<p><strong>实现中遇到的问题</strong></p>
<p><strong>当其它dex文件中的类引用了patch.dex中的类时，会出现校验错误。拆分dex的很多类都不是在同一个dex内的，怎么没有问题？</strong></p>
<p>因为这个校验有个前提，当引用类被打上了CLASS_ISPREVERIFIED标志，那么就会进行dex的校验。</p>
<p><strong>CLASS_ISPREVERIFIED标志是什么时候被打上去的？</strong></p>
<ul>
<li>在dex转换成odex（dexopt过程）时，当apk在安装的时候，apk中的classes.dex会被虚拟机（dexopt）优化成odex文件，然后才会拿去执行。</li>
<li>虚拟机在启动的时候，会有许多的启动参数，其中一项就是verify选项，当verify选项被打开时，doVerify变量为true，那么就会执行dvmVerifyClass进行类的校验，如果校验成功，这个类会被打上CLASS_ISPREVERIFIED标志。</li>
</ul>
<p><strong>具体的校验过程是怎么样的？</strong></p>
<p>有两步验证：</p>
<p>1、验证clazz -&gt; directMethods方法，其包含以下方法：</p>
<ul>
<li>static方法</li>
<li>private方法</li>
<li>构造函数</li>
</ul>
<p>2、clazz -&gt; virtualMethods</p>
<ul>
<li>虚函数 = override方法</li>
</ul>
<p>如果以上方法中直接引用到的类（第一层级关系，不会进行递归搜索）和clazz<strong>都在同一个dex中</strong>的话，那么这个类就会被打上CLASS_ISPREVERIFED标志。</p>
<p>为了解决补丁方案中遇到的问题，所以必须从这些方法中入手，防止类被打上CLASS_ISPREVERIFIED标志。空间的方案是往所有类的构造函数里面插入一段代码：</p>
<pre><code>If (ClassVerifier.PREVENT_VERIFY) {
    System.out.println(AntilazyLoad.class);
}
</code></pre><p>其中AntilazyLoad类会被打包成单独的hack.dex，这样当安装apk的时候，classes.dex中的类都会引用一个在不同dex中的AntilazyLoad类，这样就防止类被打上了CLASS_ISPREVERIFILED标志，只要没被打上这个标志的类都可以进行打补丁操作。</p>
<p><strong>注意：</strong></p>
<ul>
<li>1、在应用启动进行加载时，AntilazyLoad类所在的dex包必须先加载进来，不然AntilazyLoad类会被标记为不存在，即使后续加载了hack.dex包，那么它也是不存在的。</li>
<li>2、当在Application的onCreate中加载hack.dex时，Application不能插入上述代码。</li>
</ul>
<p><strong>为什么要选择构造函数？</strong></p>
<p>因为他不增加方法数，一个类即使没有显示的构造函数，也有一个隐式的默认构造函数。</p>
<p><strong>如何更高效地插入上述代码？</strong></p>
<p>可以使用ASM/javaassist库来进行字节码插入代码。</p>
<p><strong>Art的处理</strong></p>
<p>Art采用了新的方式，插桩对代码的执行效率没有影响。但是补丁中的类出现修改类变量或者方法，可能会导致出现内存地址错乱的情况。</p>
<p><strong>原因：</strong></p>
<p>dex2oat时fast*已经将类能确定的各个地址写死。如果运行时补丁包的地址出现改变，原始类去调用时就会出现地址错乱。</p>
<p><strong>解决方法：</strong></p>
<p>将其父类以及调用类的所有类都加入到补丁包中。</p>
<p><strong>虚拟机在安装期间为类打上CLASS_ISPREVERIFIED标志是为了什么</strong>？</p>
<p>为了提高性能。</p>
<p><strong>禁用CLASS_ISPREVERIFIED是否会影响APP的性能</strong>？</p>
<p>由于现在很多App都使用了MultiDex分包方案，这导致了很多类都没有被打上这个标志，所以此时禁用所有类打上CLASS_ISPREVERIFIED标志对性能的影响不是很大。</p>
<p><strong>如何有效地生成补丁包</strong>？</p>
<ul>
<li>1、在正式版本发布的时候，会生成一份缓存文件，里面记录了所有class文件的MD5值，还有一份mapping混淆文件。</li>
<li>2、在后续的版本中使用-applaymapping选项，应用正式版本的mapping文件，然后计算编译完成的class文件的MD5和正式版本进行比较，把不相同的class文件打包成补丁包。</li>
</ul>
<p><strong>Qzone方案缺点：</strong></p>
<p>在补丁包大小与性能损耗上有一定的局限性。</p>
<h5 id="5、补充：ASM字节码插桩"><a href="#5、补充：ASM字节码插桩" class="headerlink" title="5、补充：ASM字节码插桩"></a>5、补充：ASM字节码插桩</h5><p>插桩就是将一段代码插入或者替换原本的代码。<br>字节码插桩就是在我们的代码编译成字节码（Class）后，在Android下生成dex之前修改Class文件，修改或者增强原有代码逻辑的操作。</p>
<p><strong>原理：</strong></p>
<p>利用注解来标记需要插桩的方法，结合编译后操作字节码来帮助我们自动插入，当不需要时，关闭插桩即可。</p>
<p>除了Javassist框架外，还有一个应用更为广泛的ASM框架同样也是字节码操作框架，Instant Run包括AspectJ就是借助ASM来实现各自的功能。</p>
<p>JSON对于GSON就类似于字节码Class对于Javassist/ASM。</p>
<p><strong>Android ASM自动埋点方案实践</strong></p>
<p><strong>原理：</strong></p>
<p>Android 1.5.0版本以后提供了Transform API，允许第三方Plugin在打包dex文件之前的编译过程中操作.class文件，我们做的就是实现Transform进行.class文件遍历拿到所有方法，修改完成后对文件进行替换。</p>
<p><strong>流程：</strong></p>
<p><strong>1、自动埋点追踪，遍历所有文件更换字节码</strong></p>
<pre><code>AutoTransform -&gt; transform -&gt; inputs.each {TransformInput input -&gt; input.jarInput.each { JarInput jarInput -&gt; … } input.directoryInputs.each { DirectoryInput directoryInput -&gt; … }}
</code></pre><p><strong>2、Gradle插件实现</strong></p>
<pre><code>PluginEntry -&gt; apply -&gt; def android = project.extensions.getByType(AppExtension)
<p>registerTransform(android) -&gt; AutoTransform transform = new AutoTransform</p>
<p>android.registerTransform(transform)<br>
</code></pre><p><strong>3、使用ASM进行字节码编写</strong></p></p>
<p><strong>ASM框架核心类</strong></p>
<ul>
<li>ClassReader：读取编译后的.class文件。</li>
<li>ClassWriter：重新构建编译后的类。</li>
<li>ClassVisitor：拜访类成员信息。</li>
<li>AdviceAdapter：实现MethodVisitor接口，拜访方法的信息。</li>
</ul>
<p>1、visit -&gt; 在ClassVisitor中根据判断是否是实现View$OnClickListener接口的类，只有满足条件的类才会遍历其中的方法进行操作。</p>
<p>2、在MethodVisitor中对该方法进行修改</p>
<pre><code>visitAnnotation -&gt; onMethodEnter -&gt; onMethodExit
</code></pre><p>3、先在java文件中编写要插入的代码，然后使用ASM插件查看对应的字节码，根据其用ASM提供的Api一一对应地把代码填进来。</p>
<h5 id="6、Tinker"><a href="#6、Tinker" class="headerlink" title="6、Tinker"></a>6、Tinker</h5><p><strong>原理：</strong></p>
<ul>
<li>全量替换新的Dex</li>
<li>在编译时通过新旧两个Dex生成差异patch.dex。在运行时，将差异patch.dex重新跟原始安装包的旧Dex还原为新的Dex。由于比较耗费时间与内存，放在后台进程:patch中，为了补丁包尽可能小，微信自研了DexDiff算法，它深度利用Dex的格式来减少差异的大小。</li>
</ul>
<p>DexDiff的粒度是Dex格式的每一项，BsDiff的粒度是文件，AndFix/Qzone的粒度为class。</p>
<p><strong>缺点：</strong></p>
<ul>
<li>1、占用Rom体积，1.5倍所修改Dex大小 = Dex.jar + dexopt文件。</li>
<li>2、一个额外的合成过程，合成时间长短和额外的内存消耗也会影响最终的成功率。</li>
</ul>
<p><strong>热补丁方案对比</strong></p>
<p>若不care性能损耗与补丁包大小，Qzone是最简单且成功率最高的方案。</p>
<h5 id="7、完善的热补丁系统构建"><a href="#7、完善的热补丁系统构建" class="headerlink" title="7、完善的热补丁系统构建"></a>7、完善的热补丁系统构建</h5><p>一、网络通道</p>
<p>负责将补丁包交付给用户，包括特定用户和全量用户。</p>
<p>1、pull通道</p>
<p>在登录/24小时等时机，通过pull方式查询后台是否有对应的补丁包更新。</p>
<p>2、指定版本的push通道</p>
<p>在紧急情况下，我们可以在一个小时内向所有用户下发补丁包更新。</p>
<p>3、指定特定用户的push通道</p>
<p>对特定用户或用户组做远程调试。</p>
<p>二、上线与管理平台</p>
<p>快速上线，管理历史记录，以及监控补丁的运行情况。</p>
<h3 id="6、保活"><a href="#6、保活" class="headerlink" title="6、保活"></a>6、保活</h3><h4 id="1、厂商合作"><a href="#1、厂商合作" class="headerlink" title="1、厂商合作"></a>1、厂商合作</h4><h4 id="2、微信Hardcoder"><a href="#2、微信Hardcoder" class="headerlink" title="2、微信Hardcoder"></a>2、微信Hardcoder</h4><p>构建了App与系统（ROM）之间可靠的通信框架，让系统知道App的需求。</p>
<h5 id="原理："><a href="#原理：" class="headerlink" title="原理："></a>原理：</h5><ul>
<li>让App跨过Framework直接跟厂商ROM通信。</li>
<li>分为Client端和Server端，Server端由厂商系统侧自行实现。</li>
<li>它们直接采用LocalSocket方式，Hardcoder是Native实现的，使用了Linux的Socket接口实现了一套自己的LocalSocket。</li>
</ul>
<h5 id="性能提升有多少？"><a href="#性能提升有多少？" class="headerlink" title="性能提升有多少？"></a>性能提升有多少？</h5><p>平均10%~30%。</p>
<h4 id="3、OPPO-Hyper-Boost加速引擎"><a href="#3、OPPO-Hyper-Boost加速引擎" class="headerlink" title="3、OPPO Hyper Boost加速引擎"></a>3、OPPO Hyper Boost加速引擎</h4><p>一种优化资源调度的技术。</p>
<h5 id="原理：-1"><a href="#原理：-1" class="headerlink" title="原理："></a>原理：</h5><p>让应用程序与系统资源实现实时”双向对话”。当来自应用和游戏程序的不同场景和用户行为被Hyper Boost识别后，手机会智能地匹配到合理的系统资源，让手机SoC的CPU、GPU、ISP、DSP提供的运算资源更加合理地利用，从而让用户使用手机更加流畅。</p>
<h2 id="六、总结"><a href="#六、总结" class="headerlink" title="六、总结"></a>六、总结</h2><h3 id="1、优化总方针"><a href="#1、优化总方针" class="headerlink" title="1、优化总方针"></a>1、优化总方针</h3><ul>
<li>异步、延迟、懒加载</li>
<li>技术、业务相结合</li>
</ul>
<h3 id="2、注意事项"><a href="#2、注意事项" class="headerlink" title="2、注意事项"></a>2、注意事项</h3><h4 id="1、cpu-time和wall-time"><a href="#1、cpu-time和wall-time" class="headerlink" title="1、cpu time和wall time"></a>1、cpu time和wall time</h4><ul>
<li>wall time（代码执行时间）与cpu time（代码消耗CPU时间），锁冲突会造成两者时间差距过大。</li>
<li>cpu time才是优化方向，应尽力按照systrace的cpu time和wall time跑满cpu。</li>
</ul>
<h4 id="2、监控的完善"><a href="#2、监控的完善" class="headerlink" title="2、监控的完善"></a>2、监控的完善</h4><ul>
<li>线上监控多阶段时间（App、Activity、生命周期间隔时间）。</li>
<li>处理聚合看趋势。</li>
<li>收敛启动代码修改权限。</li>
<li>结合CI修改启动代码需要Review通知。</li>
</ul>
<h4 id="3、常见问题"><a href="#3、常见问题" class="headerlink" title="3、常见问题"></a>3、常见问题</h4><h5 id="1、启动优化是怎么做的？"><a href="#1、启动优化是怎么做的？" class="headerlink" title="1、启动优化是怎么做的？"></a>1、启动优化是怎么做的？</h5><ul>
<li>分析现状、确认问题</li>
<li>针对性优化（先概括，引导其深入）</li>
<li>长期保持优化效果</li>
</ul>
<h5 id="2、是怎么异步的，异步遇到问题没有？"><a href="#2、是怎么异步的，异步遇到问题没有？" class="headerlink" title="2、是怎么异步的，异步遇到问题没有？"></a>2、是怎么异步的，异步遇到问题没有？</h5><ul>
<li>体现演进过程</li>
<li>详细介绍启动器</li>
</ul>
<h5 id="3、启动优化有哪些容易忽略的注意点？"><a href="#3、启动优化有哪些容易忽略的注意点？" class="headerlink" title="3、启动优化有哪些容易忽略的注意点？"></a>3、启动优化有哪些容易忽略的注意点？</h5><ul>
<li>cpu time与wall time</li>
<li>注意延迟初始化的优化</li>
<li>介绍下黑科技</li>
</ul>
<h5 id="4、版本迭代导致的启动变慢有好的解决方式吗？"><a href="#4、版本迭代导致的启动变慢有好的解决方式吗？" class="headerlink" title="4、版本迭代导致的启动变慢有好的解决方式吗？"></a>4、版本迭代导致的启动变慢有好的解决方式吗？</h5><ul>
<li>启动器</li>
<li>结合CI</li>
<li>监控完善</li>
</ul>
<p>至此，探索Android启动速度优化的旅途也应该告一段落了，如果你耐心读到最后的话，会发现要想极致地提升App的性能，需要有一定的广度，如我们<strong>引入了始于后端的AOP编程来实现无侵入式的函数插桩</strong>，也需要有一定的深度，从前面的探索之旅来看，<strong>我们先后涉及了Framework层、Native层、Dalvik虚拟机、甚至是Linux IO和文件系统相关的原理</strong>。因此，我想说，Android开发并不简单，即使是App层面的性能优化这一知识体系，也是需要我们不断地加深自身知识的深度和广度。</p>
<h5 id="参考链接："><a href="#参考链接：" class="headerlink" title="参考链接："></a>参考链接：</h5><hr>
<p>1、<a href="https://time.geekbang.org/column/article/73651" target="_blank" rel="external">Android开发高手课之启动优化</a></p>
<p>2、<a href="https://yq.aliyun.com/articles/672750" target="_blank" rel="external">支付宝客户端架构解析：Android<br>客户端启动速度优化之「垃圾回收」</a></p>
<p>3、<a href="https://yq.aliyun.com/articles/680526" target="_blank" rel="external">支付宝 App 构建优化解析：通过安装包重排布优化 Android 端启动性能</a></p>
<p>4、<a href="https://github.com/facebook/redex" target="_blank" rel="external">Facebook Redex字节码优化工具</a></p>
<p>5、<a href="https://blog.csdn.net/tencent_bugly/article/details/51821722" target="_blank" rel="external">微信Android热补丁实践演进之路</a></p>
<p>6、<a href="https://zhuanlan.zhihu.com/p/20308548" target="_blank" rel="external">安卓App热补丁动态修复技术介绍</a></p>
<p>7、<a href="https://blog.csdn.net/fishmai/article/details/52398485" target="_blank" rel="external">Dalvik Optimization and Verification With dexopt</a></p>
<p>8、<a href="https://github.com/Tencent/Hardcoder" target="_blank" rel="external">微信在Github开源了Hardcoder，对Android开发者有什么影响？</a></p>
<p>9、<a href="http://mobile.yesky.com/433/311567433.shtml" target="_blank" rel="external">历时三年研发，OPPO 的 Hyper Boost 引擎如何对系统、游戏和应用实现加速？</a></p>
<p>10、<a href="https://blog.csdn.net/coder_pig/article/details/80031285" target="_blank" rel="external">抱歉，Xposed真的可以为所欲为</a></p>
<p>11、<a href="https://blog.csdn.net/balian8/article/details/53319580" target="_blank" rel="external">墙上时钟时间 ，用户cpu时间 ，系统cpu时间的理解</a>  </p>
<p>12、《Android应用性能优化最佳实践》</p>
<p>13、<a href="https://mp.weixin.qq.com/s/QVOYF2nfoWMCbM5YsxQgRQ?" target="_blank" rel="external">必知必会 | Android 性能优化的方面方面都在这儿</a></p>
<p>14、<a href="https://coding.imooc.com/class/308.html" target="_blank" rel="external">极客时间之Top团队大牛带你玩转Android性能分析与优化</a></p>
<p>15、<a href="https://github.com/zeshaoaaa/LaunchStarter" target="_blank" rel="external">启动器源码</a></p>
<p>16、<a href="https://github.com/lanshifu/MultiDexTest" target="_blank" rel="external">MultiDex优化源码</a></p>
<p>17、<a href="https://github.com/AndroidAdvanceWithGeektime/Chapter07" target="_blank" rel="external">使用gradle自动化增加Trace Tag</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android user-agent解读及使用]]></title>
        <id>https://elianworld.github.io/post/androiduseragent/</id>
        <link href="https://elianworld.github.io/post/androiduseragent/">
        </link>
        <updated>2020-12-09T08:59:20.000Z</updated>
        <content type="html"><![CDATA[<p>ua是用来让网络协议的对端来识别发起请求的用户代理软件的应用类型、操作系统、软件开发商以及版本号。</p>
<hr>
<h3 id="使用">使用</h3>
<p>可以在ua中增加自定义的一些字段来标识特殊信息。<br>
获取UA：</p>
<pre><code>String str = webView.getSettings().getUserAgentString()
</code></pre>
<p>编辑后，重设UA：</p>
<pre><code>webView.getSettings().setUserAgentString(str);
</code></pre>
<h3 id="字段解读">字段解读</h3>
<p>User-Agent的语法如下：</p>
<pre><code>User-Agent: &lt;product&gt; / &lt;product-version&gt; &lt;comment&gt;
</code></pre>
<ul>
<li>product：产品识别码</li>
<li>product-version：产品版本号</li>
<li>comment：产品信息的注释</li>
</ul>
<p>下面一个是从移动端Chrome浏览器获取的UA信息：</p>
<pre><code>Mozilla/5.0 (Linux; Android 10; V1832A Build/QP1A.190711.020; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/78.0.3904.96 Mobile Safari/537.36
</code></pre>
<p>其中</p>
<ul>
<li>Mozilla/5.0是一个通用标记符号，用来表示与 Mozilla 兼容，现在市场上绝大多数浏览器都会有这个标识（爬虫除外）</li>
<li>Linux表示浏览器所运行的原生系统平台（Windows、Mac、Linux 或 Android）</li>
<li>Android 10表示Android系统版本号</li>
<li>V1832A Build/QP1A.190711.020表示手机信息</li>
<li>wv：在Android5.0（包含）以上的系统中，会新增wv字段，与之前的浏览器做区分</li>
<li>AppleWebKit/537.36 浏览器内核及版本信息</li>
<li>(KHTML, like Gecko) 浏览器兼容性</li>
<li>Version/4.0 用来区分是Android版Chrome浏览器还是WebView字段，Android版Chrome浏览器不带有此字段</li>
<li>Chrome/78.0.3904.96 Chrome版本号，Android4.4以后才添加到ua中</li>
<li>Mobile 表示运行在手机上，以此字段区分手机、平板等，判断手机模式的正则表达式：<code>'Android' + 'Chrome/[.0-9]* Mobile'</code>，平板模式的正则表达式：<code>'Android' + 'Chrome/[.0-9]* (?!Mobile)'</code></li>
<li>Safari/537.36表示对safari的兼容性及兼容的版本信息</li>
</ul>
<hr>
<h3 id="常见浏览器的ua">常见浏览器的UA</h3>
<div class="t-big-margin">
<h1 class="t-big-margin font-18">PC端</h1>
<table class="table-bordered table">
<thead>
<tr>
<th style="width: 180px">浏览器</th>
<th>User-agent</th>
</tr>
</thead>
<tbody id="pcd">
<tr><td>safari 5.1 – MAC</td><td class="copy-agent-con">Mozilla/5.0 (Macintosh; U; Intel Mac OS X 10_6_8; en-us) AppleWebKit/534.50 (KHTML, like Gecko) Version/5.1 Safari/534.50</td></tr><tr><td>safari 5.1 – Windows</td><td class="copy-agent-con">Mozilla/5.0 (Windows; U; Windows NT 6.1; en-us) AppleWebKit/534.50 (KHTML, like Gecko) Version/5.1 Safari/534.50</td></tr><tr><td>IE 9.0</td><td class="copy-agent-con">Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0;</td></tr><tr><td>IE 8.0</td><td class="copy-agent-con">Mozilla/4.0 (compatible; MSIE 8.0; Windows NT 6.0; Trident/4.0)</td></tr><tr><td>IE 7.0</td><td class="copy-agent-con">Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 6.0)</td></tr><tr><td>IE 6.0</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)</td></tr><tr><td>Firefox 4.0.1 – MAC</td><td class="copy-agent-con"> Mozilla/5.0 (Macintosh; Intel Mac OS X 10.6; rv,2.0.1) Gecko/20100101 Firefox/4.0.1</td></tr><tr><td>Firefox 4.0.1 – Windows</td><td class="copy-agent-con">Mozilla/5.0 (Windows NT 6.1; rv,2.0.1) Gecko/20100101 Firefox/4.0.1</td></tr><tr><td>Opera 11.11 – MAC</td><td class="copy-agent-con">Opera/9.80 (Macintosh; Intel Mac OS X 10.6.8; U; en) Presto/2.8.131 Version/11.11</td></tr><tr><td>Opera 11.11 – Windows</td><td class="copy-agent-con">Opera/9.80 (Windows NT 6.1; U; en) Presto/2.8.131 Version/11.11</td></tr><tr><td>Chrome 17.0 – MAC</td><td class="copy-agent-con"> Mozilla/5.0 (Macintosh; Intel Mac OS X 10_7_0) AppleWebKit/535.11 (KHTML, like Gecko) Chrome/17.0.963.56 Safari/535.11</td></tr><tr><td>傲游（Maxthon）</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1; Maxthon 2.0)</td></tr><tr><td>腾讯TT</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1; TencentTraveler 4.0)</td></tr><tr><td>世界之窗（The World） 2.x</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1)</td></tr><tr><td>世界之窗（The World） 3.x</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1; The World)</td></tr><tr><td>搜狗浏览器 1.x</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1; Trident/4.0; SE 2.X MetaSr 1.0; SE 2.X MetaSr 1.0; .NET CLR 2.0.50727; SE 2.X MetaSr 1.0)</td></tr><tr><td>360浏览器</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1; 360SE)</td></tr><tr><td>Avant</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1; Avant Browser)</td></tr><tr><td>Green Browser</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1)</td></tr></tbody>
</table>
<h1 class="t-big-margin font-18">移动设备端</h1>
<table class="table-bordered table">
<thead>
<tr>
<th style="width: 180px">浏览器</th>
<th>User-agent</th>
</tr>
</thead>
<tbody id="mbd">
<tr><td>safari iOS 4.33 – iPhone</td><td class="copy-agent-con">Mozilla/5.0 (iPhone; U; CPU iPhone OS 4_3_3 like Mac OS X; en-us) AppleWebKit/533.17.9 (KHTML, like Gecko) Version/5.0.2 Mobile/8J2 Safari/6533.18.5</td></tr><tr><td>safari iOS 4.33 – iPod Touch</td><td class="copy-agent-con">Mozilla/5.0 (iPod; U; CPU iPhone OS 4_3_3 like Mac OS X; en-us) AppleWebKit/533.17.9 (KHTML, like Gecko) Version/5.0.2 Mobile/8J2 Safari/6533.18.5</td></tr><tr><td>safari iOS 4.33 – iPad</td><td class="copy-agent-con">Mozilla/5.0 (iPad; U; CPU OS 4_3_3 like Mac OS X; en-us) AppleWebKit/533.17.9 (KHTML, like Gecko) Version/5.0.2 Mobile/8J2 Safari/6533.18.5</td></tr><tr><td>Android N1</td><td class="copy-agent-con"> Mozilla/5.0 (Linux; U; Android 2.3.7; en-us; Nexus One Build/FRF91) AppleWebKit/533.1 (KHTML, like Gecko) Version/4.0 Mobile Safari/533.1</td></tr><tr><td>Android QQ浏览器 For android</td><td class="copy-agent-con"> MQQBrowser/26 Mozilla/5.0 (Linux; U; Android 2.3.7; zh-cn; MB200 Build/GRJ22; CyanogenMod-7) AppleWebKit/533.1 (KHTML, like Gecko) Version/4.0 Mobile Safari/533.1</td></tr><tr><td>Android Opera Mobile</td><td class="copy-agent-con"> Opera/9.80 (Android 2.3.4; Linux; Opera Mobi/build-1107180945; U; en-GB) Presto/2.8.149 Version/11.10</td></tr><tr><td>Android Pad Moto Xoom</td><td class="copy-agent-con"> Mozilla/5.0 (Linux; U; Android 3.0; en-us; Xoom Build/HRI39) AppleWebKit/534.13 (KHTML, like Gecko) Version/4.0 Safari/534.13</td></tr><tr><td>BlackBerry</td><td class="copy-agent-con"> Mozilla/5.0 (BlackBerry; U; BlackBerry 9800; en) AppleWebKit/534.1+ (KHTML, like Gecko) Version/6.0.0.337 Mobile Safari/534.1+</td></tr><tr><td>WebOS HP Touchpad</td><td class="copy-agent-con"> Mozilla/5.0 (hp-tablet; Linux; hpwOS/3.0.0; U; en-US) AppleWebKit/534.6 (KHTML, like Gecko) wOSBrowser/233.70 Safari/534.6 TouchPad/1.0</td></tr><tr><td>Nokia N97</td><td class="copy-agent-con"> Mozilla/5.0 (SymbianOS/9.4; Series60/5.0 NokiaN97-1/20.0.019; Profile/MIDP-2.1 Configuration/CLDC-1.1) AppleWebKit/525 (KHTML, like Gecko) BrowserNG/7.1.18124</td></tr><tr><td>Windows Phone Mango</td><td class="copy-agent-con"> Mozilla/5.0 (compatible; MSIE 9.0; Windows Phone OS 7.5; Trident/5.0; IEMobile/9.0; HTC; Titan)</td></tr><tr><td>UC无</td><td class="copy-agent-con"> UCWEB7.0.2.37/28/999</td></tr><tr><td>UC标准</td><td class="copy-agent-con"> NOKIA5700/ UCWEB7.0.2.37/28/999</td></tr><tr><td>UCOpenwave</td><td class="copy-agent-con"> Openwave/ UCWEB7.0.2.37/28/999</td></tr><tr><td>UC Opera</td><td class="copy-agent-con"> Mozilla/4.0 (compatible; MSIE 6.0; ) Opera/UCWEB7.0.2.37/28/999</td></tr></tbody>
</table>
<h1 class="t-big-margin font-18">Android 系统下的微信</h1>
<table class="table-bordered table">
<thead>
<tr>
<th style="width: 180px">浏览器</th>
<th>User-agent</th>
</tr>
</thead>
<tbody id="wxInAndroid">
<tr><td>小米在WIFI</td><td class="copy-agent-con">Mozilla/5.0 (Linux; Android 7.1.1; MI 6 Build/NMF26X; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/57.0.2987.132 MQQBrowser/6.2 TBS/043807 Mobile Safari/537.36 MicroMessenger/6.6.1.1220(0x26060135) NetType/WIFI Language/zh_CN</td></tr><tr><td>锤子OD103型号 4G</td><td class="copy-agent-con">Mozilla/5.0 (Linux; Android 7.1.1; OD103 Build/NMF26F; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/53.0.2785.49 Mobile MQQBrowser/6.2 TBS/043632 Safari/537.36 MicroMessenger/6.6.1.1220(0x26060135) NetType/4G Language/zh_CN</td></tr><tr><td>锤子SM919型号 WIFI</td><td class="copy-agent-con">Mozilla/5.0 (Linux; Android 6.0.1; SM919 Build/MXB48T; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/53.0.2785.49 Mobile MQQBrowser/6.2 TBS/043632 Safari/537.36 MicroMessenger/6.6.1.1220(0x26060135) NetType/WIFI Language/zh_CN</td></tr><tr><td>VIVO X6S WIFI</td><td class="copy-agent-con">Mozilla/5.0 (Linux; Android 5.1.1; vivo X6S A Build/LMY47V; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/53.0.2785.49 Mobile MQQBrowser/6.2 TBS/043632 Safari/537.36 MicroMessenger/6.6.1.1220(0x26060135) NetType/WIFI Language/zh_CN</td></tr><tr><td>HUAWEI TAG-AL00 4G</td><td class="copy-agent-con">Mozilla/5.0 (Linux; Android 5.1; HUAWEI TAG-AL00 Build/HUAWEITAG-AL00; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/53.0.2785.49 Mobile MQQBrowser/6.2 TBS/043622 Safari/537.36 MicroMessenger/6.6.1.1220(0x26060135) NetType/4G Language/zh_CN</td></tr><tr><td>小程序</td><td class="copy-agent-con">Mozilla/5.0 (Linux; Android 7.1.1; MI 6 Build/NMF26X; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/57.0.2987.132 MQQBrowser/6.2 TBS/043807 Mobile Safari/537.36 MicroMessenger/6.6.1.1220(0x26060135) NetType/4G Language/zh_CN MicroMessenger/6.6.1.1220(0x26060135) NetType/4G Language/zh_CN miniProgram</td></tr></tbody>
</table>
<h1 class="t-big-margin font-18">IOS IOS 系统下的微信 User
Agent</h1>
<table class="table-bordered table">
<thead>
<tr>
<th style="width: 180px">浏览器</th>
<th>User-agent</th>
</tr>
</thead>
<tbody id="wxInIos">
<tr><td>iphone手机OS 9_3_2版本 4G</td><td class="copy-agent-con">Mozilla/5.0 (iPhone; CPU iPhone OS 9_3_2 like Mac OS X) AppleWebKit/601.1.46 (KHTML, like Gecko) Mobile/13F69 MicroMessenger/6.6.1 NetType/4G Language/zh_CN</td></tr><tr><td>iphone手机OS 11_2_2版本 4G</td><td class="copy-agent-con">Mozilla/5.0 (iPhone; CPU iPhone OS 11_2_2 like Mac OS X) AppleWebKit/604.4.7 (KHTML, like Gecko) Mobile/15C202 MicroMessenger/6.6.1 NetType/4G Language/zh_CN</td></tr><tr><td>iphone手机OS OS 11_1_1版本 WIFI</td><td class="copy-agent-con">Mozilla/5.0 (iPhone; CPU iPhone OS 11_1_1 like Mac OS X) AppleWebKit/604.3.5 (KHTML, like Gecko) Mobile/15B150 MicroMessenger/6.6.1 NetType/WIFI Language/zh_CN</td></tr><tr><td>iphoneX WIFI</td><td class="copy-agent-con">Mozilla/5.0 (iphone x Build/MXB48T; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/53.0.2785.49 Mobile MQQBrowser/6.2 TBS/043632 Safari/537.36 MicroMessenger/6.6.1.1220(0x26060135) NetType/WIFI Language/zh_CN</td></tr></tbody>
</table>
</div>]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android 一套代码打出多个APP安装包]]></title>
        <id>https://elianworld.github.io/post/androidproductflavors/</id>
        <link href="https://elianworld.github.io/post/androidproductflavors/">
        </link>
        <updated>2020-12-04T03:37:34.000Z</updated>
        <content type="html"><![CDATA[<p>代码结构大致相同的APP，如何维护一套代码，打出不同的APP：</p>
<p>这在Android官方叫做<a href="https://developer.android.com/studio/build/build-variants?utm_source=android-studio#product-flavors">产品变种</a></p>
<p>具体配置应用如下：<br>
在主工程build.gradle文件的android下，增加以下配置：</p>
<pre><code>productFlavors {
        APPONE {
            applicationId &quot;com.test.appone&quot;
            signingConfig signingConfigs.config_one
            //代码中所需根据不同应用配置的不同参数
            buildConfigField &quot;String&quot;, &quot;AppName&quot;, &quot;\&quot;appone\&quot;&quot;
            //Manifest文件中所需根据不同应用配置的不同参数
            manifestPlaceholders = [
                    APP_NAME: &quot;appone&quot;,
                    APP_ICON: &quot;@mipmap/ic_launcher&quot;
            ]

        }

        APPTWO {
            applicationId &quot;com.test.apptwo&quot;
            signingConfig signingConfigs.config_two
            //代码中所需根据不同应用配置的不同参数
            buildConfigField &quot;String&quot;, &quot;AppName&quot;, &quot;\&quot;apptwo\&quot;&quot;
            //Manifest文件中所需根据不同应用配置的不同参数
            manifestPlaceholders = [
                    APP_NAME: &quot;apptwo&quot;,
                    APP_ICON: &quot;@mipmap/ic_launcher2&quot;
            ]

        }
</code></pre>
<p>productFlavors就是针对不同的APP变种，配置的不同的信息。</p>
<hr>
<p>buildConfigField字段配置的内容最终会编译到BuildConfig文件中：<br>
<img src="https://elianworld.github.io//post-images/1607054593718.png" alt="" loading="lazy"><br>
代码中引用方式如下：<br>
<code>BuildConfig.AppName</code><br>
如果没引到刚添加的buildConfigField，rebuild一下工程即可。</p>
<hr>
<p>manifestPlaceholders字段配置的是在Manifest文件中引用的配置:</p>
<pre><code>android:icon=&quot;${APP_ICON}&quot;
android:label=&quot;${APP_NAME}&quot;
</code></pre>
<p>特别的，可以再&quot;&quot;中进行字符串拼接，如下所示：</p>
<pre><code>android:authorities=&quot;${package_name}.provider&quot;
</code></pre>
<p>配置完成后，打包的时候就可以看到四个选项了：<br>
<img src="https://elianworld.github.io//post-images/1607055163351.png" alt="" loading="lazy"><br>
另外，在工程内直接运行时，需要根据不同的需求，在Build Variants中选择不同的构建类型，选完后会rebuild一遍工程，重新点击运行，运行到手机上的就是对应选择后的APP了。<br>
<img src="https://elianworld.github.io//post-images/1607055328314.png" alt="" loading="lazy"></p>
]]></content>
    </entry>
</feed>